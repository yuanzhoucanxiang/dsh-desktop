  await waitFor("!!document.querySelector('.dshWmMessageAction')")
  await evaluate("Array.from(document.querySelectorAll('.dshWmMessageAction')).at(-1).click()")
  await waitFor("!!document.querySelector('[data-wm-memory-save=candidate]')")
  async function setField(selector,text) {
    await evaluate(`(()=>{const el=document.querySelector(${JSON.stringify(selector)});Object.getOwnPropertyDescriptor(el.tagName==='TEXTAREA'?HTMLTextAreaElement.prototype:HTMLInputElement.prototype,'value').set.call(el,${JSON.stringify(text)});el.dispatchEvent(new Event('input',{bubbles:true}));})()`)
  }
  const memoryFile = path.join(project,'state/writing-memory.json')
  const memory = ()=>JSON.parse(fs.readFileSync(memoryFile,'utf8'))
  await setField('.dshWmMemoryCompose .dshWmSearch','灯塔停电后，备用发电机也无法启动。')
  await waitFor("document.querySelector('[data-wm-memory-save=candidate]')?.disabled===false")
  await button('存为候选')
  await waitFor("!!document.querySelector('[data-status=proposed]')")
  assert.equal(memory().items[0].status,'proposed')
  assert.equal(memory().items[0].source.kind,'assistant')
  await button('确认')
  await waitFor("!!document.querySelector('[data-status=confirmed]')")
  await button('收起备忘')
  await turn('按目前确认的设定，停电后备用发电机能启动吗？')
  await button('项目备忘')
  await waitFor("!!document.querySelector('[data-status=confirmed]')")
  await button('编辑')
  await setField('.dshWmMemoryEdit .dshWmSearch','备用发电机可以正常启动，只是她还没走到开关前。')
  await evaluate("Array.from(document.querySelectorAll('.dshWmMemoryEdit button')).find(b=>b.textContent==='保存').click()")
  await waitFor("!document.querySelector('.dshWmMemoryEdit')")
  await button('收起备忘')
  await turn('我刚在项目备忘里改了一处。按当前设定，备用发电机是什么状态？')
  await button('项目备忘')
  await waitFor("!!document.querySelector('[data-status=confirmed]')")
  await button('撤回')
  await waitFor("!!document.querySelector('[data-status=retracted]')")
  await button('收起备忘')
  await turn('现在备用发电机能不能启动，这已经定下来了吗？')
  fs.copyFileSync(memoryFile,path.join(temp,'memory-after-scene2.json'))
  results.push('Real candidate UI: proposed -> confirmed -> edited -> retracted; response semantics need review')
  console.log('LIVE_SCENE_2_COMPLETE',temp)
  await evaluate("document.querySelector('.dshWmEditor').setSelectionRange(0,document.querySelector('.dshWmEditor').value.length)")
  await button('＋ 引用稿件 / 选区')
  await waitFor("!!document.querySelector('.dshWmReference')")
  await turn('先不确定发电机的设定。请根据这段引用，把停电后声音浮现的感觉写进第一章，另存为 draft/novel/第1章-v2.md，保留 v1。只需要一个短开头，直接完成改稿。')
  const v2=path.join(project,'draft/novel/第1章-v2.md')
  assert.ok(fs.existsSync(v2),'Explicit rewrite must produce the new version')
  assert.equal(fs.readFileSync(doc,'utf8'),'灯塔的影子落在信封上。她没有拆开那封信。')
  assert.ok(fs.readFileSync(v2,'utf8').length>30,'New draft must contain actual prose')
  results.push('Real tool rewrite created v2 and preserved original v1')
  console.log('LIVE_SCENE_3_COMPLETE',temp)
