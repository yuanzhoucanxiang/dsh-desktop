# 世界观实现整改复核（2026-09-20）

结论：A01–A12 工程修复完成；完整 verify:writing-all 21 步退出码 0。总体仍 PARTIAL：真实模型五场景等尚未验收。未提交、推送、发布或替换正式安装。

基线 HEAD a9aa8b801df0ad79c245274139d5f02468d14c8b，测试对象为当前未提交工作树；版本号仍 0.1.40，不代表这些修复已进入该 Release。

| 编号 | 修复及正向证据 |
|---|---|
| A01 | shared 纯函数统一在 lib/world-setting.js，浏览器源码 re-export；新增 host 依赖闭包门禁；真包 17 文件一致、冷启动通过 |
| A02 | 世界观面板使用 createElement 正确传 children；真实 Electron 可见候选/编辑器，新增 composer 在视口内断言 |
| A03 | 校验 revision/etag 与业务输入后才提交迁移；过期与非法请求不改旧文件，备份保留原始字节 |
| A04 | host 对完整嵌套请求计算规范 hash，不信任客户端 hash；每次动作独立操作 ID，丢响应重试复用原请求 |
| A05 | 卡片按稳定本地 ID 更新，仅采纳本操作 receipt.itemId；两候选保存不串写 |
| A06 | 主记录保存与投影结果分别呈现；投影失败可重试、查看差异、明确保留手稿后重建 |
| A07 | 整理前冻结真实会话选择及 SHA256；丢弃模型伪造 sources，记录实际回复来源 |
| A08 | 修改已确认设定保留旧生效版本，明确确认后更新；支持读取候选、撤回、历史与修订 |
| A09 | 未受管文件含标记语句或空文件也不覆盖；显式保留操作生成独立手稿副本 |
| A10 | 数据/迁移/投影备份目录逐级 realpath 校验；实际 junction 越界探针拒绝 |
| A11 | 投影读取、写入与元数据同锁；先持久化 intent 再写文件，故障注入及双进程争用通过 |
| A12 | 整理使用 preparedTurn 对象；拒绝/不确定不自动重发；只采纳匹配本次作者消息后的新最终回复 |

补齐：同窗口未保存多行编辑刷新恢复，来源查看、已存候选再确认、撤回与历史修订；结构化设定不再进入旧通用备忘编辑器。停止等待只停止本地候选采纳，不冒称取消原生模型回合。

## 证据

- full-gate.log：最终完整门禁输出，含原聊天/阅读/native/UI 回归。
- world-settings-repair：10 组正向协议测试，含真实两个 Node 进程竞争。
- verify-writing-world-ui：5 组真实 Electron + host 测试；模型返回为 fixture，不能冒充真实模型验收。
- world-ui.png：最终真实窗口渲染截图。
- package.json：最终真包发布集合核对；verify:writing-packaged 9/9（冷启动、种子、主题开启与恢复）。
- 最终包：%TEMP%/wm-world-repaired-final-20260920/win-unpacked。
- 原 review/ 负向报告保留；本目录为修复后证据，不把历史失败改为当时通过。

## 仍待验收与限制

W24 真实模型五场景、两个真实 UI 窗口同时修改世界观、NSIS 隔离安装、macOS 实机、受控离线、项目移动迁移未验收。窗口草稿采用 sessionStorage，仅证明同一窗口刷新恢复，未承诺关闭窗口后找回。投影保护防止已观察到的文件变化；任意外部编辑器与最终 rename 的极小竞态不宣称彻底消除。

署名：Codex
