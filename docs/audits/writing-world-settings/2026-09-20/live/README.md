# 真实模型与双窗口验收

2026-09-20，隔离项目灯塔来信；真实原生 Harness、官方 deepseek-v4-flash、reasoningEffort=max，无模型/传输 mock。未改真实作品，凭据只注入子进程环境，未保存到证据。

按原方案 W24 对照（Codex 根据实际输出判断，非用户创作批准）：

| 原场景 | 结果与依据 |
|---|---|
| 模糊想法逐步成形 | PASS：先自由讨论，再选择真实消息整理，产生带来源候选；普通讨论不写设定，候选不自动确认 |
| 作者中途改主意 | PASS：改为期待救援船后模型顺应人物方向，确认的禁航规则不被改写 |
| 两个设定相互矛盾 | PASS：22点普通船自由出港与21点禁航冲突被明确指出；模型未擅自变更权威内容 |
| 修改已确认设定 | PASS：作者UI改为23点，历史保留21点版本，模型真实续聊按23点回答 |
| 长说明超过自动预算 | PASS：超过6000码点说明完整落盘；生产buildPreparedTurn排除说明标记且保留结论/边界；真实续聊正确回答23点与救援例外 |

共5次普通模型对话＋1次模型整理；本次整理一次得到可解析候选，1/1，不代表所有模型稳定性。实际输入见 scenarios.txt/extra.txt，输出见 initial-run.log/extra-run.log，整理原文见 organization.txt。没有捏造用户人工批准；判断由本轮审查 agent 作出。

两个真实Electron窗口共用同一host/project：窗口一保存后，窗口二过期保存显示冲突，保留未保存内容，服务器仍为窗口一版本；明确比较后再确认才更新，窗口一重新读取看到新版本，历史保留。两组通过，见 two-window-results.json、two-window-memory.json 与截图。

本轮只关闭测试脚本自行创建的窗口和内核；用户预览继续运行。

署名：Codex

模型质量观察：本次整理返回13条候选，包含规则、讨论中的建议及未决问题；不是13条已确认事实。作者需要筛选和修订，测试只确认了禁航规则。产品保留候选标记和显式确认流程，没有把推敲内容自动升级为生效设定。
