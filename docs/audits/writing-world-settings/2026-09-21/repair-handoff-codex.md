# D01–D04 修复阶段交回（2026-09-21）

状态：局部协议与实际界面探针通过，最终完整门禁 / 新真包 / 可见预览待统一验收。未提交、未推送、未发布，未替换正式版。

## 实测结果

2026-09-21 19:07，`electron docs/audits/writing-world-settings/2026-09-21/repair-ui.cjs` exit 0，四项 PASS。结果见同目录 `repair-ui-results.json`，截图 `repair-ui.png`。测试使用隔离目录、真实 Harness host 和 Electron 窗口，不调用真实模型。

| 项 | 实现与已验证行为 |
|---|---|
| D01 | 世界观窗口编辑同步保留浏览器副本，异步写 host checkpoint；清空浏览器缓存并正常关闭窗口后，新窗口可由作者选择恢复 host 中的候选，已保存设定仍在。未落盘时保留提示与关闭保护。 |
| D02 | `modelMark` 与 `pending` 随候选保存；另一真实窗口重新打开后仍显示未决标记，不提升为确认态。探针注入候选标记，仅证明持久化。 |
| D03 | 坏 JSON / 非法结构不再当作不存在；自动保存拒绝覆盖。作者确认后，校验原文件哈希，保留原字节 `.bak` 再保存当前编辑；界面与文件内容均已验证。 |
| D04 | 作者选择已不存在的旧项目位置，把聊天与设定草稿复制到独立恢复桶；保留源文件，重试不覆盖恢复后编辑。实际按钮流程与历史会话入口已验证。 |

D04 的边界：没有把旧原生会话静默绑定到新目录；历史入口仍对应旧工作区。探针的历史会话为 fixture，只验证入口存在，未证明历史会话可实际打开。真实模型与真实旧会话切换不计入本次四项通过。

此前 `plugin/writing-mode/test/draft-reliability.mjs` 六组协议断言通过，覆盖损坏恢复、非法结构、候选标记、移动恢复、恢复幂等及仍存在的旧目录拒绝。该结果先于共享目录的 V 系列后续改动，最终验收必须复跑。

## 并行编辑交界

用户已确认另一位 agent 正在同一仓库修改。发现新增 `lib/project-identity.js`，以及 `lib/file-lock.js` / `lib/draft-checkpoints.js` 中 V4/V6/V7 改动。本轮没有回滚这些修改。

19:07 之后暂停修改共享源码、生成产物、总日志与策划案，避免覆盖。最新 client 构建和四项 UI PASS 是当时工作树结果，不是最终稳定版本证明。

交回后需优先复核以下边界（待核实，不冒充已复现缺陷）：

- 项目身份规范化改变哈希桶键时，对既有尾分隔符桶的兼容读取与迁移，防止旧 checkpoint 再次失联。
- `quarantineStaleLock` 的检查与 rename 之间是否可能移走替换后的活锁；是否会用于自动清扫，是否与既有“不抢锁”契约一致。
- 列表上限与移动恢复的交互：UI 可分页或限量，但完整迁移不可无提示漏掉旧桶。
- 新模块必须进入 runtime manifest，并由依赖闭包与真包检查实际覆盖。
- 世界观 journal 的面板重挂载、写入队列、存储失败与晚到回包保持作者编辑；当前已加每项目复用 journal 实例，最终需回归。
- 截图人工复看发现：备忘面板同时打开时，世界观编辑区被挤压为很小的滚动区域，恢复成功提示也有裁切。四项功能断言通过不能代替可用性验收；统一收尾时需修正右栏高度分配，并验证常见窗口尺寸下编辑与恢复入口可见。

## 下一步统一验收

1. 等并行源码交回，固定待验收内容，核对上述兼容与并发边界。
2. 更新 CONTRACT、策划案及镜像、CHANGELOG、当日日志和 WORKLOG；区别已发布能力与本轮未发布修复。
3. 重建 client，跑协议、四项 UI 与完整 `verify:writing-all`；保留旧审查负向证据，不以新结果覆盖旧结论。
4. 在 `%TEMP%` 重建 Windows dir 包，核对依赖与 manifest、隔离冷启动及打包门禁。
5. 启动可见隔离预览并保持运行，由用户关窗；不关闭现有正式桌面或其他预览。

— Codex
