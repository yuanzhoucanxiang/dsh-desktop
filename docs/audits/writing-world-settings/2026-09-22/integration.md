# 写作模式整改与已有项目接入：整合验收

日期：2026-09-22；署名：Codex。当前源码基于 HEAD 6783647 的未提交工作树，保留并整合 ox-alpha 的并行修复。未提交、未推送、未发布，也未替换用户正式应用。

## 本轮结论

CXR01/CXR02 正向复核通过：在线不移走他人的锁；同名/同内容文件不充当作品身份。已有项目接入已从独立副本整合，顶栏“打开已有”把所选目录作为项目，保留原目录；文档、会话、备忘共用该项目身份。修正完整路径分组带来的 draft 优先排序回归。

世界观恢复增加重复点击和卸载防护。扩展实际 Electron 测试，分别验证真正移动目录与无关联手动导入，验证第 5 份以后按需加载、网络失败重试、等待期间新编辑以及关闭面板后迟到响应。原始历史探针保留，本轮结果另存，不用负向探针失败冒充所有问题已修。

## 实跑证据

| 检查 | 结果 |
|---|---|
| CXR fix-verification | 25 项，0 失败 |
| verify:writing-all | 24 步执行完成，包括下面各项；详见 integrated-evidence/gate.log |
| hardening | 92 通过 |
| 旧正向 UI 基线 | 32 条通过 |
| 已有项目协议 | 6/6 |
| 已有项目 Electron UI | 5/5；便携 fixture 与真实资料副本分别实跑 |
| 综合整改 UI | 8/8；repair-integrated-ui-results.json |
| 冷启动与升级 | 12/12 |
| 真包启动、首启种子、主题切换/恢复 | 9/9 |
| 显式 --package 字节检查 | 8 PASS、0 FAIL、1 待跑（真实 profile 漂移，未手动同步） |
| 用户原始资料 | 13/13 与测试副本字节一致；未创建 project.md，未自动写入已确认设定 |

门禁中的发布检查默认不带 --package，已另行带最终包路径补跑；真实 profile 与工作树有 8 个文件不同，不能称用户正式环境已升级。综合门禁结束后，只调整 UI 测试等待页面稳定再截图，并重跑真实资料测试 5/5；未再改运行时代码。截图已实际查看，包含目录分组、世界定调文档和专用写作伙伴栏。

## 最终包与可见预览

包目录：`%TEMP%/wm-existing-integrated-20260922/win-unpacked`。包内 19 个插件文件逐字节核对通过，主进程 11 个模块与源码一致。完整 asar 和每个插件文件 SHA256 见 integrated-evidence/package-identity.json；插件在 extraResources 中，不能只凭 asar 哈希判断插件版本。

最终包预览 PID 55108，实际窗口标题 DeepSeek Harness，保持运行，由用户关闭。详情见 integrated-preview.json；同时隔离 DSH_HOME、DSH_DESKTOP_HOME、userData、LOCALAPPDATA。项目是原始资料副本，不是原目录。

试用：进入写作模式 → 选择“赫尔帝国 · 隔离副本” → 打开设定档案或世界定调 → 切换主线骨架 → 在写作伙伴中讨论。可另用顶栏“打开已有”选择自己的测试目录。预览未自动发送模型请求。

## 仍开放

- 残留锁在线只诊断，会阻塞该桶；离线维护与未来真正原子所有权机制仍有成本，不宣称彻底解决所有文件系统竞争。
- 稳定作品 ID 尚未引入；路径记录只能提供当前协议定义的关联证据。
- 本轮未重新跑真实模型五场景、完整 IPC 通道面、同一 Release 清单到下载资产的哈希闭环、NSIS 隔离安装及 macOS 实机。
- 世界观恢复已实测关闭面板；跨项目切换期间延迟响应未另列独立 UI 用例。
- 自定义目录递归有扫描上限，会提示未完整；只支持当前 Markdown 阅读/编辑范围。

策划案 v0.12 与 E:/剧本/写作模式-策划案.md 同步，CONTRACT、README、WORKLOG、CHANGELOG、当日日志已补充。旧日期记录为历史时点，不冒充当前发布状态。
