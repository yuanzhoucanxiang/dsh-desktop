# v0.1.38 发布验收

- 版本提交 / tag：`ebe452c7bf075797c8cea3fcf715bcb004c01fc5` / `v0.1.38`。
- Windows 构建目录：`%TEMP%\dsh-release-0.1.38-20260912-124620`。临时 builder 配置重定向输出，未在工作区生成新 asar。
- selector-check 通过；写作 host/controller 16 项、编辑器 4 组、聊天 fixture 4 组、真实内核 3 组通过。未调用真实模型。
- 打包版 `--smoke`：SMOKE_OK；app.asar 中版本为 0.1.38，writing-mode 的 11 个资源与源码 SHA256 一致。
- Windows 安装包 229,782,818 字节；latest.yml 的版本、文件长度、SHA512 与本地安装包一致，远端 latest.yml 回下载与本地一致。各文件 SHA256 见 windows-checksums.json。
- macOS 构建 workflow `34673962641` 成功，head SHA 与发布 tag 一致；Windows 机器未进行 macOS 原生启动测试。

## 旧 UI 冒烟断言与当前实现

不能把本次原有 `--ui-smoke` 输出写成全绿：

1. 全新隔离 home 未装内置主题时，有三个主题断言失败。`--ui-smoke` 会在 `seedBuiltinsIfNeeded()` 之前退出，测试不会执行正常首启的安装步骤。
2. 使用生产 `runSeed` 安装打包内置插件后，仅两个旧断言失败：读取 html 颜色（当前主题令牌在 body）、找 `#palis-theme-crt`（当前节点为 `.palis-crt-sweep`）。原检查仍未修改。
3. 随附 `verify-theme.cjs` 在同一打包 exe 的 --ui-smoke 运行期间通过 CDP 读取当前实现：主题开启时 body 颜色为 #0a0a0a 且 `.palis-crt-sweep` 存在；关闭时属性和 CRT 均移除。还验证原 UI 测试仅上述两项失败。结果见 theme-proof.json。
4. 后续应更新 main.js 内旧 UI 冒烟测试口径及其首次安装准备；本次未改主题实现或为了测试改动发行包。

— Codex / GPT-6，2026-09-12

远端复核：Windows 三项 asset digest/size 与本地逐一一致，Release 共 5 个 uploaded 资产，清单见 release-assets.json。macOS 更新清单的版本、文件名、大小与远端 DMG 元数据匹配；DMG 全量回下载在本机超过 4 分钟仍未收到正文，已停止这一个冗余 gh 下载进程，不影响已发布资产。没有把 macOS 的 SHA512 本地回下载交叉验证或真机启动记为通过。
