# 工作日志 · 2026-09-14

## 〔153〕 写作模式整体重新评估：5a152dc

- 按用户要求对照原架构方案整体复评，HEAD 5a152dc；1664467 后只有文档变化。重新跑 32 条 UI 正向时序全过，build verify PASS，没有重新打开已关闭的故障单。
- 只读核查实际 unpacked 包与隔离 profile：13 个插件文件均与仓库 SHA256 一致，app.asar/main.js 字节一致，包中 host 入口可导入。报告所列 exe 路径当前未检测到进程，未擅自重启/关闭任何桌面。
- 架构结论：稳定性返工已收口，原方案尚未完成。entry 3528 行且 src/client 仅单文件，原生会话直接接触 UI；备忘 UI、当轮参考控制仍欠缺；preparedTurn 丢弃传入的 memoryRevision，memoryHint 未接入；CONTRACT 的恢复承诺和 API 表过时。
- 最新打包报告明确保留架构/30 项/模型等开放项。本次认可包字节进展，但预置 profile 不证明空 home 自动同步/离线冷启动/旧版安装升级；32 时序不是原方案 30 项。
- 新增 docs/audits/writing-architecture/2026-09-14/overall-assessment.md、两份复评探针与两份 JSON，同步 CHANGELOG/WORKLOG。建议顺序 A/B 依赖边界 → D/E 作者可控记忆 → 30 项及打包预览 → 契约/交接。
- 未改生产实现、未合并/发版、未调用真实模型，未提交/推送审查材料。所有新执行的 UI 测试使用 TEMP 隔离数据。

— 署名：Codex / GPT-6

---

## 〔154〕 写作模式下一阶段执行方案 v2

- 按用户要求制定 docs/plans/writing-mode-next-execution-v2.md，基线 5a152dc，作为原 v1 的实施补充；明确这是计划，未实施生产改动。
- 四批：P1 模块/现有 esbuild 构建与资源清单；P2 Harness adapter、跨窗口创建协调及 uncertain 恢复；P3 备忘候选/编辑/历史、引用恢复和当轮上下文控制；P4 原 30 项、空/旧 home、离线/安装验证和可见隔离预览。
- 具体约定包括状态权威、原生接触边界、来源和 memoryRevision、失败重试、旧引用兼容、锁恢复边界、受管资源验证、模型体验单列、交回目录及可复制启动指令。复用 review9 已通过行为，禁止重新引入旧数据/错误状态问题。
- 工程按批自主推进，技术自检不要求用户逐批确认；写作产品保持自然交流和按需工具，不增加固定轮数、每轮记忆审批或后台陪聊。交付止于实现/证据/可见预览，不自动合并 main 或发布。
- 同步 CHANGELOG/WORKLOG。本轮只写方案与日志，未运行无关生产测试、未修改实现、未提交或推送。

— 署名：Codex / GPT-6

## 写作模式 P1（模块与构建）——执行进度

按 `docs/plans/writing-mode-next-execution-v2.md` 执行 P1，顺序：构建 → 职责提取 → 边界网。
分支 `feat/writing-mode-architecture`，起点 HEAD `5a152dc`。

### 已完成（每步都跑既有回归，全绿）

| 提交 | 内容 |
| --- | --- |
| `0df2361` | 归档复评 + 方案 v2 + 日志（仅文档，声明不含生产能力） |
| `10ed908` | 构建改 esbuild 真实模块打包；entry.js 工厂体 → ESM 模块 |
| `70971ab` | 提取 styles / copy / services(writing-api) |
| `704dfcb` | 提取 state/companion-drafts（19 导出，模块头写明 X01 等约束） |
| `d8806a8` | 提取 adapters/harness（runtime getters + sessions 搬迁） |
| `802ff1c` | 构建接线：prestart/predev + build-macos.sh 显式调用 |

`entry.js` 行数：3526 → 3062 → 2820 → 2781。产物 client.js 153,003 字节、
两次构建字节一致、无绝对路径。

### 新增边界网 `npm run verify:writing-architecture`

8 项规则：R1 客户端不引 Node 内置；R2 shared 不依赖 React/DOM/适配器/服务；
R3 adapters 不含 React/JSX/组件/样式；R4 相对 import 全部可解析；
R5 运行时清单（`runtime-manifest.json`：11 个文件存在、lib 下 8 个模块全部登记、
测试专用 `lib/editor-session.js` 显式排除、不得含 src/ 或重复登记）；
R6 入口不再持有样式数组与草稿保存队列。

### 途中三个自己踩的坑（都靠"切完立刻构建/校验"逮住）

1. 切 copy.js 时原块的 `T`/`pickLocale` 没删净 → **重复声明**（esbuild 报错才发现）。
2. `writing-api.js` 的 `api` 漏了 `export` → import 解析失败。
3. 剪枝脚本用 `new RegExp('\\b'+name)` 经 shell 转义后 `\b` 变成字面量 → 所有名字被误判
   "未使用"，写出空 import 块 → 构建失败。**教训：涉及正则/反斜杠的脚本一律写成文件跑，
   不要走 bash 内联**（今天已是第二次同类）。

### 回归证据（改动后每次都跑）

`verify:writing-build` PASS · `writing-f` 10 · `writing-cde` 25 · `writing-p1` 7 ·
`verify:writing` 16/16 · `review5-protocol` 9 · `review7-status` 10 ·
`verify:writing-architecture` 8 项 PASS。

### 剩余 P1

拆 UI 组件到 `features/{editor,library,companion,memory,tools,settings}` + `app/WritingModeApp`；
清单接入打包/同步路径（repo→包→profile 逐文件比对）；`verify:writing-ui/chat/native`
进正式回归入口（需隔离 Electron 实例）。

署名：ox-alpha

## 写作模式 P1 续（UI 拆分完成）——entry 3526 → 165 行

### 搬迁结果（每步都构建 + R1–R7 + 回归）

| 新模块 | 内容 |
| --- | --- |
| `state/prefs-store.js` | 偏好 store：versionOf/getPrefs/subscribePrefs/loadPrefs/savePrefs + DEFAULT_PREFS |
| `state/mode-store.js` | 写作开关与 body 属性：applyBodyAttr/readActiveLS/modeActive/setModeActive/subscribeMode + LS_KEY |
| `features/memory/index.js` | loadProjectMemory + CompanionMemoryPanel |
| `features/companion/index.js` | 伙伴 store hooks/companionRows/CompanionTranscript/CompanionChat/WritingCompanion |
| `features/settings/entries.js` | 侧栏/头部入口组件 |
| `features/settings/WritingModeSettings.js` | 设置面板（373 行） |
| `app/dom-float.js` | DOM 常驻浮动入口 + domFloatEl |
| `app/WritingModeApp.js` | 主工作台（1640 行：三栏布局/库/编辑器/工具编排） |

`entry.js` 现在只剩：守卫、name/inject、apply(ctx) 装配与测试钩子导出（165 行）。

### 运行期故障两例（都只有 E2E 能发现，静态检查漏过）

1. **`LS_KEY` TDZ 被 try/catch 吞掉**：`const LS_KEY` 被插到 `let modeActive = readActiveLS()` 之后，
   `readActiveLS` 的 try/catch 把 ReferenceError 吞成 `false` → `modeActive=false` → 应用整块
   `return null`（overlay 空、无任何报错）。**教训：被 try/catch 包住的 TDZ 是静默故障**。
2. **`WritingCompanion` 漏 import**：app 用了 companion 模块的组件但未导入 → 运行期
   `ReferenceError`（浏览器里才有）。R7 检查器**漏报了它**（owner 表与用法扫描单测都正常，
   边界仍未定位）——**结论：这类"运行期未定义"以 E2E 为权威**，静态检查只作辅助。

### 验证证据（P1 验收）

- 4 套 node 单测：`writing-f` 10 · `writing-cde` 25 · `writing-p1` 7 · `verify:writing` 16/16
- 另两套既有：`review5-protocol` 9 · `review7-status` 10
- **3 套 Electron E2E 全绿**：`verify:writing-ui` 4 · `verify:writing-chat` 4 · `verify:writing-native` 3
  （隔离 userData/DSH_HOME，不碰用户应用；正是它们逮住上面两个运行期故障）
- 构建：产物 154474 字节 / 3663 行，两次构建字节一致；`verify:writing-build` PASS
- 边界：`verify:writing-architecture`（R1–R6 + R7）全 PASS

### P1 剩余

1. `app/WritingModeApp.js`（1640 行）内部仍混着 editor/library/tools —— 方案目标结构里它们是
   独立 feature 目录，需再拆（本次先把"整段样式/功能面板/保存队列"清出 entry 的硬要求做到了）。
2. ~~`runtime-manifest.json` 已建，但尚未接入打包/profile 同步路径~~ → 已完成，见下方「P1-②」条目。
3. R7 的漏报盲点需定位（或改为 bundle 后校验 + 以 E2E 为准）。

署名：ox-alpha

## 写作模式 P1-②（运行清单接入打包与 profile 同步）

上一条遗留第 2 项：`runtime-manifest.json` 已建但没接进任何路径。本次把它变成**唯一发布集合**。

### 改了什么

| 文件 | 改动 |
| --- | --- |
| `lib/plugin-sync.js`（新） | 清单驱动同步：`publishSet`（清单优先，无清单退回扩展名递归）· 逐文件 sha256 比对 · 受管记录 `.dsh-managed.json` · 安全清理 |
| `lib/plugin-sync.test.js`（新） | 10 项单测（发布集合/幂等/清理边界/比对判据） |
| `main.js` | `syncBuiltinPlugin` 从"按扩展名全量递归覆盖"改为 `pluginSync.syncPluginDir`（约 -30 行手写遍历） |
| `plugin/writing-mode/runtime-manifest.json` | 加 `root`（清单基准）与 `docs`（CONTRACT.md）；清单自身也随包发布 |
| `package.json` | 打包 filter 补 `runtime-manifest.json`、排除 `!lib/editor-session.js`（与清单逐文件对齐） |
| `scripts/verify-writing-package.mjs`（新） | 三段验收：清单↔打包 filter（静态）· 清单↔安装包产物 · 清单↔profile |

### 清理的安全边界（方案硬要求）

只删「上一版受管记录里存在 + 这一版不再发布 + 磁盘内容仍与上次写入哈希一致」三者同时成立的文件。
用户改过的（哈希不符）→ 保留并进 `keptEdited`；没有受管记录的历史文件（如老版本全量递归种进去的
`test/`、`src/`）→ **一律保留**并计入 `foreign` 上报——无法证明归属就不动（方案原文要求）。

### 证据

- 单测 `plugin-sync` 10/10（含 R5 用真实迁移场景：受管 test/ 文件被正确清理；R6/R8 用户文件与历史遗留一律保留）
- 静态：`verify:writing-package` 报 **manifest ↔ 打包 filter 逐文件一致（13 个文件）**
- 外壳级实测（`--smoke` + 隔离 DSH_HOME/userData，`patchMode=full` 会走同步）：
  - 空 profile 首同步：`writing-mode ... [manifest] +13`，落成**恰好 13 个文件 + 受管记录**，
    不再把 `test/`、`src/`、`lib/editor-session.js` 种进用户机器；另两个无清单插件走 `[fallback] +3`
  - 升级路径（喂入机上那份旧式 20 文件副本）：`[manifest] +1 ~1 -0 (未变 11)` ——补齐
    `runtime-manifest.json`、把漂移的 `client.js` 换成当版（hash 实测一致），旧 `test/`、`src/` 8 个文件
    **保留并记日志**「非受管文件保留 8 个（…）」
- 回归：`verify:writing-architecture`/`verify:writing-build`/`verify:writing`(16/16)/`writing-f` 10/`writing-cde` 25/
  `writing-p1` 7 全绿；三套 Electron E2E 全绿
- 新增正式回归入口 `npm run verify:writing-all`（把上面全部串成一条命令，不再依赖临时脚本）

### 顺带发现

三套 E2E **都不跑 `main.js`**（`verify-writing-native` 自己 spawn 内核，ui/chat 是纯渲染器探针），
所以它们验证不了外壳同步路径——`--smoke` + 隔离 DSH_HOME 才是这条路径的探针。已按此取证。

署名：ox-alpha

## 写作模式 P1-③（R7 漏报盲区：定位并修好）

上一条遗留第 3 项。这次把它查到根因，并给 R7 配了**自身敏感性自检**。

### 根因

给 R7 加了 `--debug <名字>`（逐条规则复算"是谁把它当成声明"）后复现定位：
摘掉 `WritingCompanion` 的 import 后，R7 仍判它"已声明"，而且指出是**参数位置**规则。

```js
...text.matchAll(/\(([^)]*)\)\s*=>/g)   // ← 病灶
```

`[^)]*` **可以跨行**：某段跨 3 行的箭头参数括号里正好包着 `jsx.jsx(WritingCompanion, {…})` 的用法，
于是整个用法被当成"箭头形参"，`WritingCompanion` 进了本模块的声明集合 → 静默放行。
（同一类病灶还有 `/\{([^}]*)\}/`、`/\[([^\]]*)\]/` 的解构提取，都会跨行吞代码。）

### 修法

新增 `balanced(text, openIndex)`：**严格括号配对**（栈式，遇不匹配的闭括号即保守结束），
形参/箭头参数/catch 参数/解构全部改为"配对后取内部标识符"，`[^)]*` 一类跨行正则全部移除。

### 证据（自检脚本 `scripts/verify-writing-imports.selftest.mjs`）

把 `src/client` 复制到临时目录 → 注入漏 import → 期望 R7 失败并点名；另有阴性对照与复原校验：

| 场景 | 结果 |
| --- | --- |
| 阴性对照（未注入） | PASS（无误报） |
| 主应用漏 import 组件（历史漏报现场） | **FAIL 并点名 WritingCompanion**（修好前是静默 PASS） |
| 记忆 feature 漏 import 服务模块 | FAIL 并点名 api |
| companion 从多名 import 里漏一个（最隐蔽形态） | FAIL 并点名 getDraftStatus |
| 复原后 | PASS |

自检已并入 `npm run verify:writing-architecture`（R1–R6 + R7 + R7 自检）。真机 `verify:writing-imports` 仍 PASS。

**教训**：只会说 PASS 的检查器不可信 —— 凡是"检查器"，都要有一条"注入缺陷后它必须报错"的证据。

署名：ox-alpha
