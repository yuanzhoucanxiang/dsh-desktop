# 2026-09-25 · dsh-desktop

## 修复 Windows 发布包丢失应用图标（v0.1.42 资产已替换）

### 现象与根因

用户报告安装后的应用图标变成 Electron 默认图标。根因：自 v0.1.39 起，本地发布为绕开
electron-builder 在 packaging 阶段连 GitHub 下载 electron 的问题，构建配置里带了
`win: { signAndEditExecutable: false }`。这个开关跳过的不只是签名，还有**往 exe 写入图标与
版本信息的资源编辑步骤**——于是 0.1.39/0.1.40/0.1.41/0.1.42 的 Windows 包全部带着默认图标
（macOS dmg 由 GitHub Actions 用正常配置构建，不受影响；NSIS 安装器自身的图标由 makensis
编译时嵌入，也不受影响）。

### 修法

1. **重建 v0.1.42**（用户当前所在版本）：`git worktree add` 到 `v0.1.42` tag（ac00c15，
   不混入工作区里未发布的 hotkey 改动）→ node_modules 以 junction 复用主仓库 →
   `System32\tar` 打 runtime.tar.gz（runtime 自 8 月锁定未变，与 tag 内容一致）→
   `prepare-builtin-plugins.mjs` 产出内置插件树（pnpm 走本地 store；palis v0.5.13 起初
   下载失败、TEMP 缓存已被清理，重试后成功）→ 用 `win: { signExecutable: false }`
   **只跳过签名、保留图标/版本信息**重建 dir 包。
2. **验证**：提取新 exe 的关联图标为 PNG 与默认 electron.exe 对比（哈希不同），人工确认是
   鲸鱼 logo；`verify-writing-package --package` 在 worktree 内 8/8（含 app.asar 主进程模块
   与 tag 源码**逐字节一致**）；真包 `--smoke` → `SMOKE_OK`。NSIS + `gen-update-manifest.js`
   出 `latest.yml` 后覆盖上传 `setup.exe / .blockmap / latest.yml` 三个资产，远端 latest.yml
   的 sha512 与本机构建完全一致（`pA5yN3sA…`）。
3. **防复发**：`scripts/rebuild-package-tmp.mjs` 改为 `signExecutable: false` 并加注释说明
   两个开关的区别；本次提交不包含用户工作区里未提交的 `lib/hotkey.test.js` 改动。

### 用户侧操作

同版本不会触发自动更新（updater 比较 version 相等即跳过）：**到
<https://github.com/yuanzhoucanxiang/dsh-desktop/releases/tag/v0.1.42> 重新下载
`dsh-desktop-0.1.42-setup.exe` 再装一遍即可**（覆盖安装保留 userData/账号/会话）。
装过的旧版本（0.1.39–0.1.41）用户会经自动更新升到修复后的 0.1.42，无需单独处理。

### 坑

- Git Bash 下 `System32\tar.exe -C <带空格路径> runtime` 会把路径咬碎（`es\compiler\lib\mic`），
  必须经 PowerShell 调用（build.ps1 一直是对的）。
- 摘除 worktree 里的 node_modules junction 必须用"删链接本身"的方式
  （PowerShell `(Get-Item …).Delete()` 或 `rmdir`），`git worktree remove` 会递归删除目录、
  跟随 junction 把主仓库的 node_modules 内容一起删掉——先摘链接再 remove worktree。

署名：ox-alpha

## 设置 → 外观：应用图标自定义（含新「看板娘」预设）

需求：用户想在设置里自定义应用图标，并把工作区根的 `图标.png`（蓝发看板娘，1254×1254 RGB）作为可选之一。

### 能力边界（Windows）

- **能改**：窗口/任务栏图标（`win.setIcon`，即时生效）；桌面 / 公共桌面 / 开始菜单 / 公共开始菜单 /
  任务栏固定区的 .lnk 的 IconLocation（指到 userData 里生成的 .ico）。
- **改不了**：exe 内嵌图标（打包期资源编辑）——「默认」预设即 exe 内嵌鲸鱼；
  托盘是独立的「主题色鲸鱼」状态指示，设计上不随此设置变化（UI 里已说明）。

### 实现

- `lib/app-icon.js`（纯 Node 可测）：预设表、取值校验、**PNG 帧 ICO 容器封装器**
  （16→256 七档，nativeImage 负责缩放与编码）、快捷方式改写 PowerShell 脚本构建器
  （参数 base64 内嵌，杜绝引号注入；`-EncodedCommand` 执行）。
- `lib/app-icon.test.js` 7 项：ICO 目录项/偏移/256→0 编码、非法帧过滤、预设文件存在性、
  取值白名单、脚本 base64 还原语义、reset 语义、扫描范围。
- `main.js`：`settings.appIcon`（default/预设 key/custom）；四个窗口创建点换成动态图标；
  `shell:pick-app-icon`（设置窗口专属，文件选择）与 `shell:set-app-icon`（复制/生成/持久化/
  应用到窗口/改写快捷方式）；get-state 以 privileged 附带 `appIcon` 与缩略图（dataURL，
  对齐字段最小化原则）；启动时非默认则**重申快捷方式图标**（安装器/更新会重建 .lnk）。
- 设置 UI：新「外观」页——预设卡片（缩略图+选中态）、自定义 PNG/ICO 选择、恢复默认；
  CSP 为缩略图放开 `img-src data:`；UI 文案说明固定图标缓存与托盘不受影响。
- 资产：`build/icons/maiden.png`（用户提供的图）随 `build.icons.files` 打进包；打包前用户图不经任何修改。

### 验证与交付

- `app-icon-test` 7/7；main/preload/settings 三处语法检查通过；隔离 `--smoke` → `SMOKE_OK`。
- 可见预览实例：`%TEMP%\dsh-preview\app-icon-feature\`（隔离 home/userData/LOCALAPPDATA），
  PID **49888**，保持运行由用户试用：设置 → 外观 → 点「看板娘」即可看到窗口/任务栏图标变化，
  桌面快捷方式（若有指向本实例的）也会被改写。
- 注意：同版本自动更新不会重装，此功能将在下个版本随安装包发布。

署名：ox-alpha

### v0.1.43 发布结果（补记）

- tag v0.1.43 → 226c251（已核对 tag^{commit} == HEAD）；release published 且为 Latest。
- 资产五件齐：setup.exe（220.4 MB）/ .blockmap / latest.yml 本机上传（远端 digest 与本机 sha256 一致：c327d484…）；
  mac dmg + latest-mac.yml 由 workflow（14m40s success）自动补齐。
- 本版起安装包 exe 内嵌鲸鱼图标（已验证提取图标 ≠ Electron 默认）；外观功能随版可用。
- 坑（补记）：NSIS 若 cwd 在工作区，electron-builder 会把产物写进工作区 dist 并尝试对
  uninstaller/elevate 走 signtool（无证书必挂）。改用 API 方式 prepackaged + directories.output=TEMP +
  win.signExecutable:false 一次通过。已在此发布中固定该姿势。

署名：ox-alpha

## 移除外壳审阅侧栏 UI（改用 dsh-better-sidebar）+ 修好 better-sidebar 安装

### 背景

用户反馈外壳自带的「审阅」侧栏效果不如 dsh-better-sidebar（其内核插件的右侧栏界面），要求去掉外壳版。
查证 better-sidebar 在本机不可用的根因：profile 里的条目是指向工作区开发目录的链接（目标已消失=悬空），
内核"任一 bundle 失败即整体退出"→ 外壳隔离机制把它从 bundles 摘除保启动；种子一次性且"已存在即跳过"，
真包从未落地。修复时顺带发现 dsh-pet、@dsh-local/palis-theme-panel、cosmokit、schemastery 在 profile
node_modules 里也缺失（stage 按实际 import 闭包裁剪后，种子只跑过一次没能补全）——已全部按
安装包内置 stage 顶层逐个补齐，并把 dsh-better-sidebar 加回 bundles（只增补，不动其他条目）。

### 移除范围（界面层，谨慎保留底层）

- **preload.js**：整套 `dsh-review-*` 注入与渲染（330–1581 行，~1250 行）+ 引导调用删除；
  `PALIS_CHROME`（含断连浮层的 palis 方角样式）改由浮层自己的 style 注入——原来它搭审阅块的车。
  preload 1591 → 335 行。
- **保留内核侧 review-bridge 插件与 NDJSON 事件流**：窗口标题/托盘的「运行中」提示（agentBusy）
  由流里的 turn start/end 驱动，与审阅 UI 无关，摘了会退化。回退端点保留（loopback 闸 + 体积上限已加固）。
- **runUiSmoke**：侧栏存在性/展开/拖拽/git 视图/查看器断言全部移除，替换为
  「审阅界面已不再注入」的正向断言（与"特性已删除"同口径，不是删检查换通过——主题联动断言原样保留）。
- **删除四个只服务审阅 UI 的一次性脚本**与对应 npm 项：review-ui-check / sidebar-test /
  sidebar-layout-test / sidebar-skin-check。
- 保留：lib/git-review.js 及其测试（休眠的可测库）、panelWidth 设置字段与 IPC（无消费者，无害）。

### 验证

- main/preload 语法检查通过；`verify:ui-smoke`（含新的"已移除"断言 + 主题开启/恢复）PASS。
- better-sidebar：包已落 profile、bundles 已登记。**需重启内核生效**（托盘/菜单 → 重启内核，或重启应用）。

署名：ox-alpha

## 官方桌面端对照调研（纯文档，未改任何代码）

- **做了什么**：用户问"dsh 有官方桌面端吗"。核实结论——**有，且已公开发布**。
  官方仓库 `apps/desktop`（`@deepseek-ai/dsh-desktop`，private）+ `apps/desktop-host`，
  首现于 tag `dsh-v0.1.5-alpha.1`（2026-09-08，约 17 天历史），近三周 ≥100 次桌面提交；
  规模 src 61 文件 ≈7.1k 行、tests 202 文件 ≈16.8k 行，与我们内置的 `dsh@0.1.1-rc.1` 已形成 6 个 rc 周期的版本代差。
  安装包走**自建 CDN**（腾讯云 COS）：`https://download.deepseek.com/dsh-desk/feeds/win-x64/nightly.yml`
  实测 200，version 0.1.7-rc.2，exe 288,245,480 B，`HEAD` 该 exe → 200 / Server: tencent-cos；
  mac-arm64 zip 372,794,444 B。GitHub Release 附件为 0、仓库 README 与官网均无下载入口 → 公开可下但半保密、灰度状态。
  **更正**：本会话早前向用户判断"找不到正式下载入口"，实为"入口不在仓库而在 CDN"。
- **新增文件**：`docs/official-desktop-vs-shell-2026-09-25.md`（对照报告：官方能力清单、外部插件加载通路、
  逐条接缝清单、迁移可行性、建议路线）。**未改任何源码，未跑构建，未杀/起任何进程。**
- **关键结论（影响后续决策）**：
  ① 官方壳 `patchFiles: []`（`apps/desktop-host/src/index.ts:30`）→ 我们的 `--patch` 注入通路在官方壳上不存在，
     `review-bridge` / `shell-settings` / 首启种子 / 自研插件启停全部必断。
  ② 但 `writing-mode` 全仓 grep `dshShell` = 0 命中、只用官方 Slots + 相对路径 fetch + 回环鉴权，
     `palis-theme-panel` 只有一处已做特性探测的软依赖 → **插件本体搬得动**，改走官方插件管理器的"本地绝对路径"安装。
  ③ 官方桌面与 CLI 共享 `~/.dsh`（仅 profile/包/激活/锁隔离），NSIS 卸载**绝不动 `~/.dsh`** → 稿件数据换壳不丢。
  ④ 官方转发的唯一门是 `Origin === dsh-app://app`，无路径白名单（`web-document.ts:76-93`）
     → 任何绝对 `http://127.0.0.1:<port>` 请求必挂；我们的插件恰好全用相对路径。
  ⑤ 壳的剩余增量只剩 5 项边角（全局热键/开机自启/任务完成通知/多会话窗口/启动画面），
     而"最强对标点"外壳审阅侧栏本日已由用户判定移除 → 建议路线：**弃壳保插件**，
     第一步是在隔离环境实机验证"官方壳能否挂上 writing-mode"（需用户同意下载 288MB 安装包，未执行）。
- **坑 / 备注**：调研期间发现本仓库 HEAD 已推进到 `4cb99d3`（v0.1.43 今日发布），另有 ox-alpha 一路未提交改动
  （含四个注入 UI 检查脚本的删除）。已核对为该条日志记录的用户决定，非失控；本次调研未触碰。

署名：Qoder（第三方对照调研）

## v0.1.44 发布（审阅移除 + 种子修复机制）

- 移除外壳审阅侧栏 UI（见上节）；内置插件种子新增**修复能力**并配 8 项新测试：
  stamp 已写后，若清单插件「在 bundles 或在隔离记录」而 profile 包缺失/悬空/坏条目，
  自动补齐真包（用户卸载/禁用不顶回）；bootable 闸照常把关。
- CHANGELOG 收口 0.1.44 两条；docs/releases/v0.1.44.md；WORKLOG 补行。

署名：ox-alpha

## 官方对照调研的补测：内核 0.1.7-rc.2 实测（ZCode 会话）

- **做了什么**：核实对照报告里影响路线的三条断言（读官方克隆 + 读本仓代码），再按
  `docs/kernel-upgrade-checklist.md` 把候选内核 0.1.7-rc.2 装到 `%TEMP%` 实测。
  契约验收（独立 DSH_HOME）**12 pass / 3 skip / 0 fail → KERNEL_CONTRACT_OK**；
  把 `writing-mode`、`dialog-optimize` 各补一个 `cordis.patch.yml` + `dsh.bundle.patch`
  （= 作为 profile bundle 挂载的最小差分）后实测：宿主零加载报错、客户端真挂上
  （精确选择器：`#dsh-writing-mode-float`、`data-plugin=@dsh-local/writing-mode`、侧栏「写作模式」入口）。
- **修正报告两处**：① §7.1 的"转发后回环鉴权"风险**不成立**——官方转发器转发前删掉
  `host/origin/cookie/sec-fetch-site` 四头（`web-document.ts:84-88`），三条判据都会走"缺失即放行"；
  ② §2.2 的"未声明槽位会抛错"对我们**不适用**——四个槽位在 0.1.7-rc.2 官方槽位树里全部存在。
- **新发现**：`@deepseek-ai/dsh-client-runtime` 在 0.1.7-rc.2 已不存在，但内核容忍缺席注入项，
  且我们的客户端 bundle 零 `@deepseek-ai` import（只外部化 react）→ 无害。新内核客户端模块路由是
  合并式 `plugins/??a/client.js,b/client.js`，`verify-plugin-compat.mjs` 的路由猜测过时
  （它给的两条 client WARN 是探针口径旧，不是失败）。
- **结论**：任何前进路线都要面对的那道门（内核跳版）**是绿的**，所以"装官方 288MB 壳"不是前置条件。
  建议顺序改为：先走「保壳升内核」把控制权留住（官方桌面与内核 lockstep + 服务端强更 ⇒ 换壳等于
  内核版本不能钉，而 `writing-mode` 对内核磁盘布局有私有依赖 `~/.dsh/.agent-presets/*`）→ 壳的定位
  降为"内核版本控制器 + 交付通道"，不再往壳层加差异化能力 → 官方壳该不该日常用，降级为用户亲自试用的
  产品判断（须隔离安装）。细节见 `docs/official-desktop-vs-shell-2026-09-25.md` §10。
- **未做**：未安装官方桌面端、未改任何源码、未动现有安装与 4 个运行中进程。候选内核树留在
  `%TEMP%\dsh-candidate-0.1.7-rc.2` 供复用；探针脚本在 `%TEMP%\dsh-wm-probe\`。

署名：ZCode

## 应用协议落地：渲染进程只认 `dsh-app://`（本地直出 + 转发内核）

- **动机**：对齐官方桌面端的加载架构，摘掉旧方式的三处脆弱——窗口直连
  `http://127.0.0.1:<随机端口>/?token=…`，于是「origin 相等」只能拿端口当判据、一次性 token
  出现在渲染进程 location 里、每个静态资源都要经内核回环。
- **改动**（只在 `main.js`；`DSH_DESKTOP_SCHEME=0` 可整条退回旧方式做 A/B）：
  - 早于 app ready 注册特权 scheme（standard/secure/supportFetchAPI/corsEnabled/stream/codeCache）；
  - `protocol.handle('dsh-app')`：`/` 与 `/index.html` **取内核产出**（boot 注入是内核的事，外壳不碰），
    `/assets/**` 等 dist 内文件**本地直出**（构建产物是内容哈希 → 发 `immutable` 长缓存），
    其余**代持内核 cookie 转发**（转发前删 `host/origin/cookie/sec-fetch-*`，响应删连接级头与
    `set-cookie`——与官方 `web-document.ts` 同构）；
  - `kernelPageUrl()` → `dsh-app://app/`；导航闸门 `isKernelPageUrl` 与两处「重启后是否要重新
    loadURL」的判据由 `startsWith('http')` 改为同源判定；`indexHtmlCache` 随内核每次启动失效。
- **验证**（均在隔离实例上，未触碰在跑的应用）：
  - `npm run smoke` → `SMOKE_OK`（旧启动链完好）；`npm run verify:ui-smoke` → **PASS**（主题开启/恢复两次）。
  - CDP 实测：`location.origin = dsh-app://app`、URL 无 token、**渲染进程零 `http://127.0.0.1` 请求**；
    `/assets/*` 响应带我们的 `immutable` 头（= 本地直出），`/plugins/<id>/client.js` 带内核的 `no-cache`
    （= 转发）；插件 API `fetch('/api/writing-mode?route=config')` → **200**（插件的回环鉴权在转发链路下
    成立）；写作模式浮动入口与侧栏「写作模式」入口均在。
- **未覆盖 / 既有事实**：真实会话的长连接流（agent 输出）结构上是 `Response.body` 直通，未做压力验证；
  开发态没有 `resources/builtin-plugins` 树，种子本就不跑（非本次引入）。
- **顺带发现（不是本次引入）**：`npm run shell-hardening-test` 有一条 S6 断言仍在 preload.js 里找
  `sessionData.truncated` /「只显示最近的改动」——那是今日移除审阅侧栏时删掉的文案，该门禁当前是红的，
  属那次改动的收尾遗漏。
- 预览实例：`%TEMP%\dsh-preview\scheme-017`（隔离 userData，共享 `~/.dsh` 以便看到真实文库），
  保持运行由用户关窗；`main.js` 改动留在工作树未提交。

署名：ZCode

### v0.1.44 发布结果（补记）

- tag v0.1.44 → 334a1e9；release published 且为 Latest；五件资产齐（setup.exe 220.4 MB / blockmap / latest.yml / mac dmg / latest-mac.yml，mac workflow 14m8s success）。
- 远端 setup.exe digest 与本机 sha256 一致（c2beb7fa…）；远端 latest.yml version=0.1.44。
- 发布前验证：门禁 9 项（asar 主进程逐字节一致等）、exe 图标=鲸鱼（提取比对）、exe 元数据 ProductVersion=0.1.44、真包 9/9。
- 过程失误与拦截：v0.1.44 tag 曾指向未含版本号提升的提交（发布准备脚本写了没跑），且 mac workflow 已误触发——
  在任何错误资产上传前拦截：取消 workflow、删远端 tag、补跑准备、重新打 tag。
- 另：git add -A 把另一会话遗留的 docs/official-desktop-vs-shell-2026-09-25.md（无敏感内容的分析文档）带进了提交，已核实无害、保留。

署名：ox-alpha

## 内核升级 0.1.1-rc.1 → 0.1.7-rc.2（含两个插件的适配）

按 `docs/kernel-upgrade-checklist.md` 走完采纳流程（候选 → 契约 → 插件实测 → 采纳）。全程隔离，
未动现有安装与在跑的 4 个进程。

### 验收

- 候选树（518 包）：契约 18 条 → 13 pass / 0 critical。独立 DSH_HOME 下 `shell.profile-kernel-packages`
  的 FAIL 是「被测运行时 ≠ profile 链接指向的运行时」的已知产物（手册已写明不是升级风险）——实测确认
  profile 里 `@deepseek-ai/*` 全是指向运行时的符号链接，启动时自愈。
- 采纳后（runtime/ 重建：dsh=0.1.7-rc.2、444.7MB、maxPath 198）：
  `verify-render-invariants` → **RENDER_INVARIANTS_OK**（写作区底色、装饰层包含块、视口锚定、
  圆形宽高/DPR 2.73、合成层）；`verify-ui-smoke` → **PASS**（主题开启 × 恢复两轮）；
  `verify-plugin-compat`（新 stage 树）→ PLUGIN_COMPAT_OK。

### 两个必须适配的插件（实测得出，不能靠矩阵猜）

1. **dsh-better-sidebar 0.15.2 → 0.21.1**：0.15.2 在新内核上 `failed to import`（它 import 的
   `settingsNamespace` 已从 `@deepseek-ai/dsh-settings` 删除）。逐版本实测：0.18.1 / 0.19.1 能加载但
   客户端抛 `list slot "conversation.chat.turnTail" requires options.id`；**0.21.1 干净**。
2. **@dsh-local/palis-theme-panel 0.5.13 → 0.5.14**（本仓 + palis 仓），两处断点：
   - **裸名 `schemastery`**：生态已只用 `@deepseek-ai/schemastery`，裸名过去由旧 stage 树顶层带入；
     better-sidebar 升 0.21.1 后顶层不再有裸包 → palis `failed to import`。改带 scope 的包名
     （import / peer / build.sh 的 junction / shims.d.ts 一并）。
   - **settings 子系统重写**：新内核用 `SettingsForms`，命名空间由「插件行 id + 插件导出的 Config」推出
     （`describe()` 取 `entry.options.id`），`register(ns, schema)` 已删；且只认带 `meta.volatile` 的字段
     （未标注时 update 报 "has no volatile fields"）。移植：Config 逐字段 `volatile()`、呈现策略
     `configure({auto:false}, ctx.fiber)`、**行 id 定为 `palis-theme`** 以保住既有设置命名空间
     （否则主题开关会静默重置）。老内核没有 `.volatile()` → 按能力分支；双内核实测
     `POST /api/palis-theme → 200 {enabled:true}`（老内核回归也过）。
3. **运行期安全网的正则**：内核换了报错文案（`<entryId> (<pkg>): failed to import`；旧的
   `failed to import loader entry <…>(<pkg>)` 不再出现）→ `detectBrokenBundles` 补一条并配 5 条用例。
   顺带发现：新内核对坏 bundle 不再"整体退出"，只是该插件缺失 + 一行报错，启动照常。

### 顺带修的一处真回归（本会话早先的协议改造引入）

自定义 scheme 下 `new WebSocket('/x')` 解析成 `ws://app/…`（host 是 app，连不通）。better-sidebar 的
终端/文件监听就是这么写的，而且**新内核自己的前端也用 WS**（`/api/remote.mux`）——两者叠加会把
"新内核 + 新协议"变成必挂组合。做法：在外壳产出的首页注入一段极小的 WS 兜底（host 为 app 的目标
改写回内核端口），并把内核 cookie 镜像进渲染会话（WS 握手要带鉴权）。实测：插件式拼出的
`ws://app/sidebar/ws/fs-watch` 实际握手到 `ws://127.0.0.1:<port>/sidebar/ws/fs-watch` ✓。

### 待拍板

- **发布 palis v0.5.14**：把 `dsh-palis-theme-panel/dsh-local-palis-theme-panel-0.5.14.tgz` 改名
  `dsh-palis-theme-panel-0.5.14.tgz` 传成 GitHub Release 资产（mac CI 构建要下载它；本机已放进
  `%TEMP%\dsh-builtin-cache` 走离线路径验完了）。
- **重建发行版**（`npm run dist`，产物出工作区）→ 独立实例目检 → 发版。

改动文件：`prepare-runtime.ps1` / `prepare-runtime-macos.sh`（版本钉 + 改官方源）、`builtin-plugins.json`
（两个 pin）、`main.js`（破 bundle 正则 + WS 兜底 + cookie 镜像）、`dist/builtin-plugins`（刷新）、
palis 仓四处源码/构建文件 + 版本 0.5.14。

署名：ZCode
