# 2026-08-22

## ㉑ 架构加固 v0.1.21：把「更新出 bug」从灾难降级为可恢复

- **背景**：用户要求彻底优化架构，减少以后版本更新与内容更新出 bug。对着本 session 实战踩过的五类 bug 逐一加防护。
- **改动**（提交 49761bf，已发布 v0.1.21）：
  1. **坏插件自动隔离**：内核对 profile bundle 任一失败即整体退出（PALIS 缺 dsh.bundle、
     工作区插件解析失败都搞挂过整个应用）。`startKernelUntilReady()` 恢复链：失败时
     `detectBrokenBundles()` 从日志提取报错点名的包名（failed to import/apply loader entry
     (pkg)、profile bundle "pkg" declares；cordis:* 与核心 bundle 排除）→
     `quarantineBundles()` 从 profile 的 dsh.profile.bundles 摘除（写 userData 的
     disabled-bundles.json）→ 重试（≤4 轮）+ 系统通知；托盘「重新启用被隔离的插件」一键恢复。
     启动失败仍保留补丁降级链；「重启内核」只隔离不降补丁（慢启动不该丢审阅桥）。
  2. **运行时安全交换**：解压校验完整性（node.exe+dsh 入口+runtime.json）不完整不碰现有；
     切换走 .prev 备份→就位→失败回滚；runtimeRoot() 在主运行时损坏时回退 .prev。
  3. **日志编码**：decodeChunk()——含 U+FFFD 即非合法 UTF-8，用 GBK TextDecoder 解码内核输出。
  4. **更新器残留清理**：killStaleUpdaterInstallers() 在启动与装更新前清 dsh-desktop-updater
     目录里卡住的安装器进程（实战僵尸挡过更新）。
  5. **检查更新重试**（0s/3s/8s）；**读 profile manifest 剥 BOM**（PS5.1 `Set-Content -Encoding UTF8`
     写 BOM，内核 JSON.parse 直接崩——本次测试脚本自己踩中，顺手加了应用侧防御）。
- **实测**（在 %TEMP% 构建，工作区内 dist/dist-alt 均被 ZCode 文件监视锁住 app.asar）：
  - T1 隔离：注入缺 dsh.bundle 的假插件 → 内核崩 → 日志 `quarantined broken bundle: dsh-broken-test`
    → 自动重试 → **SMOKE_OK**；profile 里坏插件被摘、其余（dsh-pet/palis 等）完好。
  - T2 交换：删 runtime.json 制造标记不一致 → 重解压 → `.prev` 备份生成（含 node.exe）→ 原子切换 → SMOKE_OK。
  - 发布后 `--updated` 覆盖安装 0.1.20→0.1.21 退出码 0，内核就绪。
- **坑**：① PS5.1 here-string 单行写法非法；② `Set-Content -Encoding UTF8` 带 BOM 坑了内核；
  ③ ZCode 会锁工作区内新出现的构建产物 app.asar——构建输出必须放工作区外（本次用 %TEMP%）。

— 署名：deepseek-v4-flash

## ㉒ 修复：侧边栏插件消失（profile bundles 重建时丢失）

- **现象**：用户报告桌面版右侧栏（dsh-better-sidebar）没了。
- **排查**：
  - `disabled-bundles.json` 不存在 → 不是 v0.1.21 隔离机制干的；
  - `~/.dsh/profiles/web/package.json` 的 `dependencies` 里 5 个插件都在，但
    `dsh.profile.bundles` 只剩 base/web-app/super-injector/palis/dsh-pet ——
    **装 dsh-pet 与做 T1 隔离测试重写 manifest 时，把 `dsh-better-sidebar`
    和 `@nanmicoder/dsh-auto-mode` 两个条目弄丢了**（工作日志 ㉖⑮ 时代它们都在）。
- **修复**：bundles 数组补回两个条目（依赖 link 与 @deepseek-ai junction 均完好，
  两包的 `dsh.bundle.patch` 声明也都在），node 校验 JSON 通过。
- **附带发现**：`settings.json` 的 workspace 被测试改成 `dsh-desktop\.git`（遗留）。
  应用运行中会把内存 settings 写回磁盘，磁盘直改会被覆盖 → 留给用户在应用里
  Ctrl+Shift+O 重选（选完自动重启内核，一并生效）。
- **生效方式**：重启内核（托盘 → 重启内核 / Ctrl+Shift+K）。按项目规则未擅自杀
  运行中的内核进程（pid 8256）。

— 署名：deepseek-v4-flash

## ㉓ 修复：内核"提前退出"——pnpm 跨盘 link 悬空 + 隔离机制盲区

- **现象**：㉒ 补回 bundles 后用户重启应用，内核反复 `提前退出（code=1）`。
- **根因（两层）**：
  1. profile `node_modules` 里 `dsh-better-sidebar`/`@nanmicoder/dsh-auto-mode` 的
     symlink 悬空——pnpm-lock.yaml 把跨盘 link 存成相对路径
     `link:../../../Deepseek harness/...`（从 `~/.dsh/profiles/web` 解析落到
     `C:\Users\DL\Deepseek harness\`，该目录不存在；插件实际在 E 盘）。昨晚
     `dsh plugin add dsh-pet` 触发 pnpm 重链整个 node_modules，两个链接被打坏。
     dsh-app-boot 抛 `cannot resolve profile bundle "dsh-better-sidebar"` 退出。
  2. detectBrokenBundles 只认 `failed to (import|apply) loader entry` 与
     `profile bundle "x" declares`，不认 `cannot resolve profile bundle`，
     隔离机制没兜住 → 用户直接看到启动失败。
- **修复**：
  - 悬空 symlink 删除，改为 junction 指向 E 盘真实包目录（`cmd mklink` 经 Git Bash
    转义会报"卷标语法不正确"，用 PowerShell `New-Item -ItemType Junction` 成）；
  - lockfile 两条 `version:` 改回绝对 E 盘路径（**注意：下次任何 `dsh plugin add`
    后要复查这两个链接是否又被 pnpm 相对化打坏**）；
  - settings.json workspace 复位为默认（应用未运行时直改才有效；此前 `.git` 是
    ㉑ 测试遗留）；
  - main.js detectBrokenBundles 增加 `cannot resolve profile bundle "x"` 匹配
    （node --check 通过，对真实 kernel.log 命中）——此类失败以后走隔离摘除自动重试。
- **验证**：复刻应用启动命令 headless 拉起内核（--no-open）→ 无 bundle 报错、
    HTTP 200 → 测试进程已清理。palis 不在 lockfile 里（手动 junction 装的），
    未受 pnpm 影响。

— 署名：deepseek-v4-flash

## ㉔ 新增：插件体检面板（v0.1.22，用户提出"设置里加插件检测/一键查更新"）

- **方案**（对齐事故史，三档走前两档）：①只读体检（bundles 一致性/悬空链接/
  dsh.bundle 声明/隔离名单）②registry 更新比对（只查不装）③一键更新——**缓**，
  等"pnpm 操作后自动复查并重建 junction"就位再上，否则就是一键自爆按钮。
- **实现**：
  - `lib/profile-inspect.js`：纯 node 零 Electron 依赖（inspectProfile + 轻量
    semver 比较），7 种问题码（missing-entry/dangling-link/not-in-bundles/
    no-dsh-bundle/unreadable/quarantined/bundle-without-dep）；普通库不误报
    （looksLikeDshPlugin：有 dsh.bundle/dsh.client 声明或 dsh- 命名才算插件）。
  - main.js：IPC×4（report/check-updates/restore/open）+ 插件面板窗口
    （照 previewWin 配方）+ 托盘常驻入口与 ⚠ 异常入口 + 启动静默体检
    （异常时推 shell:plugin-health 给渲染侧）。调试口：DSH_DESKTOP_PLUGINS_PANEL=1
    启动自动开面板。
  - preload.js：dshShell 4 个新方法 + 主界面左下角「⚙ 插件」入口（复用审阅栏的
    --dsw-alias 令牌，跟随内核明暗主题；异常红点）。
  - renderer/plugins.{html,css,js}：面板 UI（汇总/隔离卡片+恢复/表格/更新列）。
- **测试**：fixture 14 断言全过——**顺手抓到 bundles 提取笔误**
  （`manifest.profile` 应为 `manifest.dsh.profile`，症状是所有插件误报
  not-in-bundles，恰好被单测逮住）；真实 profile 基线 ok=true（5 插件 0 异常）；
  面板截图（真实 preload 桥+桩数据）视觉核对：正常/警告/错误标签、
  「↑ 0.2.0 有新版」、隔离卡片、本地开发版标注均正确。
- **坑**：①Electron 离屏窗口（x=-20000）本机 capturePage 抛 UnknownVizError，
  改屏幕内窗口即可（splash-shot 的离屏法在这台机器不可靠）；②dev 实例静默退出
  不是 bug——单实例锁，用户应用开着时第二个实例直接 quit，完整链路待装机验证。
- 打包清单：files 增加 lib/profile-inspect.js。

— 署名：deepseek-v4-flash

## ㉕ 设置面板：软件更新页 + 更新体验重做（借鉴 lumen，仍属 v0.1.22）

- **用户需求**：检查更新放进左下角设置里；优化检查更新与更新弹窗；参考 lumen。
- **借鉴点（读 lumen src/main/updater.ts + UpdateNotes.tsx 提炼）**：
  - 状态机 idle→checking→available→downloading(%)→downloaded/error，经
    `shell:update-status` 推送渲染层；
  - autoDownload=false 由外壳编排下载（才能做网络错误静默重试 2 次/5s 间隔）；
  - **disableDifferentialDownload=true**（lumen 里程碑 99：差分=先差分一遍→失败回退
    整包再跑一遍，进度条 100% 后归零，慢网必失败）；
  - 静默检查失败递增重试 8s/30s/2min + 60s 超时保护（electron-updater 网络抖动会无限挂起）；
  - release notes 分类渲染约定：✨ 新功能 / 🐛 修复 / ⚙️ 优化 + · 条目（发版 body 按此写）。
- **面板升级**：plugins.* → settings.*（git mv），双页签「插件 / 软件更新」；
  openSettingsPanel(tab) 支持 URL 参数与 shell:settings-tab 事件切页。更新弹窗从
  模态框改为面板卡片 + 系统通知（像 Chrome/VSCode 被动提醒），托盘「检查更新…」
  直达更新页。左下角入口改名「⚙ 设置」，红点=插件异常或更新已就绪（updateGet+双事件）。
- **实现**：main.js 重写 setupAutoUpdater/checkForUpdates（旧函数删除）为状态机 +
  runUpdateCheck/downloadUpdateWithRetry/scheduleAutoCheck；新 IPC update-get/check/
  download/install；installUpdateNow 补销毁 settingsWin。preload 增 update* 桥 +
  onSettingsTab/onUpdateStatus。
- **验证**：语法全过、无残留旧引用（openPluginsPanel/pluginsWin/checkForUpdates 函数
  已全清）；截图验证更新页两态——downloaded（✓ 0.1.22 已下载完成+安装按钮+notes 分类
  渲染逐字正确+页签红点）、downloading（⬇ 正在下载+进度条 63%+百分比）。
- **待装机验证**：单实例锁导致用户应用开着时 dev 实例起不来（㉔ 已记录）。

— 署名：deepseek-v4-flash

## ㉖ E2E 实测：设置面板全链路（应用运行中旁路验证）

- **动机**：用户问"你进行测试了吗"——此前 UI 验证是桩数据，真实应用链路因单实例锁没测。
- **旁路方案**：新增两个环境变量调试口（提交见下）——`DSH_DESKTOP_USER_DATA`
  隔离数据目录（单实例锁按 userData 分桶，可在用户应用运行时起第二个隔离实例）+
  `DSH_DESKTOP_SETTINGS_SHOT` 应用进程内 capturePage 存证（外部截屏会拍到用户隐私
  窗口，不用）。capturePage 在本机硬件加速下偶发 UnknownVizError，钩子里
  show/focus + 重试 3 次解决。
- **实测结果（真实 main.js + 真实 preload + 真实 profile + 真实内核）**：
  1. 隔离实例与用户正开的应用并存，内核就绪（ready at …）；
  2. 插件页（真实数据）：五行插件版本逐一核对正确（0.3.3/0.1.0/0.1.2/0.12.2/0.1.4），
     全部 ✓ 正常、共 5 个插件 0 项异常、本地链接显示"本地开发版"；
  3. 主窗（真实内核页面）：左下角「⚙ 设置」入口注入成功（0 异常 → 无红点，正确）；
  4. 更新页（真实 IPC + 真实点击）：当前版本 v0.1.22 → 点「检查更新」→ 卡片显示
     "开发模式不支持自动更新，打包安装后生效"——runUpdateCheck 未打包分支的正确
     输出，整条 update-get/check 链路通了。
- **仍未测**：打包版真实更新流（发现新版本→下载进度→装上）——只能靠发布 v0.1.22
  后从 0.1.21 应用内升级实战检验（0.1.21 的旧更新器 + 新安装器双端都是真实路径）。

— 署名：deepseek-v4-flash

## ㉗ 发布 v0.1.22

- 构建走 %TEMP%\dsh-build-022（工作区外，避 ZCode asar 锁）：tar 打包 runtime
  （71.4MB，marker: dsh 0.1.1-rc.1 / node v26.1.0）→ electron-builder --dir
  （自定义 config：files 含 lib/profile-inspect.js，extraResources 指向 OUT 的
  归档+marker）→ app-update.yml → NSIS --prepackaged → blockmap → latest.yml
  （gen-update-manifest.js 传 exe 绝对路径即可写到 OUT）。
- asar 校验：settings.{html,js,css} ×3、lib/profile-inspect.js 在；旧 plugins.* 已不在。
- `gh release create v0.1.22`（notes 按面板分类约定：✨/🐛/⚙️ + · 条目——
  新更新页会按此格式渲染 release body）+ 上传 setup.exe/latest.yml/blockmap。
  latest 指向确认 v0.1.22；build-macos 工作流由 tag 触发跑起来了
  （0.1.21 那次是 failure，这次看结果；不影响 Windows 更新）。
- **端到端剩余验证**：用户从 0.1.21 应用内升级（托盘 → 检查更新…）——
  注意 0.1.21 装机版跑的是旧更新器（autoDownload=开、差分开），下载中断无自动
  重试；0.1.22 起才是新状态机。安装后新 UI 首启 = 新更新链路的第一次实战。

— 署名：deepseek-v4-flash

## ㉘ 0.1.22 装机 + 首次实战记录

- **应用内升级失败的完整诊断**（用户报"点检查更新没反应"）：检查其实成功
  （日志 5× update available: 0.1.22），但 0.1.21 静默下载无进度提示；本机直连
  GitHub 当天断线 36 次，190MB 只下到 13.9MB；旧更新器断线不重试、一断从头来
  → 永远到不了 100% → 界面零反馈。**教训：静默下载必须有可见进度+重试，
  0.1.22 两者都已补齐。**
- **装机**：应用内更新走不通 → 直接装本地构建包（sha512 与 GitHub release 逐字节
  一致）。用户关应用 → `setup.exe /S` 退出码 0（薄壳直装，无需先卸载——
  0.1.17 架构设计的又一次验证）→ 0.1.22.0 就位。
- **首启验证**：内核 2 秒 ready（运行时标记一致免解压）；启动静默更新检查遇
  GitHub 超时 → 日志 `retry in 30s` —— 新状态机的递增重试在真实环境第一次生效。
- 遗留观察：global hotkey 偶发 rejected（历史日志也有，非本次引入）；0.1.23
  发布后可顺带验证 0.1.22→0.1.23 的新下载链路（断线重试+进度条）。

— 署名：deepseek-v4-flash

## ㉙ 修正两处体验（按用户反馈）

- **删除主界面左下角「⚙ 设置」悬浮入口**：当初理解偏了——用户说的"左下角设置"
  指内核自带的设置入口，不是要外壳加按钮。设置面板保留，入口走托盘
  「设置（插件 / 软件更新）…」。后续若要进内核自带设置面板，需走内核侧插件
  （外壳已有 review-bridge/dialog-optimize 先例），用户没提不做。
- **palis 启动动画标题改终端开机风**：`DEEPSEEK HARNESS ARCHIVE` →
  **`DSH TERMINAL v{外壳版本}`**（用户四选一选定）。splash.js 新增 appVersion
  模块变量（preview/初始 status 两处先赋值再 applyTheme），palisTitle() 动态拼；
  打字机与 aria-label 均走它。splash-shot 截图验证：标题/光标/note/日志面板/
  进度全部正常。deep/seascape 皮肤标题不动。

— 署名：deepseek-v4-flash

## ㉚ 内核侧插件 shell-settings：设置入口进内核自带设置面板（用户："做内核侧插件"）

- **背景**：用户澄清"左下角设置"= 内核自带的设置入口（㉙ 已删外壳悬浮按钮），
  要把桌面设置/检查更新放进**内核自己的**设置面板 → 唯一正路是内核侧插件。
- **官方契约**（研究 dsh-client-ui-settings-plugin-inventory 提炼）：
  浏览器插件经 `ctx.slots.inject("settings.plugins.tab", () => ctx.slots.register({name,
  id, order, label, locale, inject}, ReactComponent))` 在设置的「插件」分区贡献标签页；
  组件 props 收 `{ t }`（绑定后的本地化函数）；client inject 列 slots/locale 服务；
  react 在模块映射里环境内置（官方包也不列）。
- **实现**：`plugin/shell-settings/`（宿主 index.js 最小可挂载 + package.json + client.js
  手写 bundle）。标签页"桌面外壳"：外壳版本 + 更新状态机展示（update-get/onUpdateStatus）
  + 检查更新/重启安装按钮 + 「打开桌面设置」→ `window.dshShell.openSettings()`（preload
  桥在内核页面主世界可用，审阅栏先例）。浏览器直连（无 dshShell）降级为提示文案。
  apply() 全程 try/catch 防御：契约变化时静默降级不拖垮激活批次。
- **接线**：BUILTIN_PLUGINS += shell-settings（writeKernelPatch 自动插行、syncBuiltinPlugin
  自动同步三件套）；package.json extraResources 增加对应 filter。plugin-selector-check
  无需改（零 DOM 选择器，纯 Slots）。
- **验证**（隔离子实例 + 真浏览器）：内核带新插件行干净启动 ready；HTTP 200 服务
  client.js（/plugins/@dsh-local/shell-settings/client.js，10.3KB）；页面内 style
  data-plugin-css 标记存在 = 客户端在内核页面成功求值。**坑**：IAB 浏览器对该页面的
  合成输入全部失效（click/keypress 都不达，Playwright click 被 actionability 拦截，
  连 Shift+Tab 焦点都不动）——设置弹窗的视觉验证做不了，待装机后用户一次点击实测
  （Electron 内点击不受影响）。

— 署名：deepseek-v4-flash

## ㉛ 发布 v0.1.23

- 构建 %TEMP%\dsh-build-023。**坑**：临时构建配置从 022 复制而来，没带新加的
  shell-settings extraResources 条目——首建产物 resources/plugin/ 缺三件套。
  修正：手工补文件进 win-unpacked 后重跑 NSIS（等价），配置存档已同步。
- 顺带发现：repo extraResources 从无 palis-theme 条目，内置 palis-theme 一直没进包
  （静默跳过）；用户内核页的 PALIS 实为 profile 里 @dsh-local/palis-theme-panel 提供，
  且 userInstalledPlugin 的 substring 匹配也让内置副本必然让位——现状无害，不动。
- `gh release create v0.1.23`（✨/⚙️ 格式）+ 三件套上传，latest 确认指向 v0.1.23。
- 装机后待用户实测：内核设置 → 插件 → 「桌面外壳」标签页（IAB 无法验证的最后一环）。

— 署名：deepseek-v4-flash

## ㉜ 用正确逻辑覆盖"崩溃后刮日志"——坏插件隔离改前置体检

- **背景**：用户要求"继续优化、debug、用正确逻辑覆盖，而不是打补丁"。审查发现
  `detectBrokenBundles` 是典型的补丁式实现——每次新失败模式就往正则列表里加一条
  （已累计 3 条：failed to import/apply、declares no dsh.bundle、cannot resolve
  profile bundle），本质是"等内核崩了再刮日志猜"。
- **正确逻辑**：静态会炸内核的问题（缺 dsh.bundle / 链接悬空 / 缺条目 / package.json
  不可读 / link 目标不存在）都能从 manifest + 文件系统**确定性判定**，不需要等崩溃。
  - `lib/profile-inspect.js` 新增 `bundleBootable(name, opts)`：单 bundle 可加载性闸门
    （node_modules 可解析 + dsh.bundle.patch 声明 + link 目标存在），不依赖是否已在
    bundles 列表（恢复隔离时它不在列表，inspectProfile 会漏判）。
  - `main.js` 新增 `findNonBootableBundles()`：启动前对启用列表逐 bundle 校验，返回
    会炸内核的名单（含原因）。
  - `startKernelUntilReady()` 恢复链重排为三层：①前置体检隔离（主防线，不启动内核）
    → ②运行期安全网（detectBrokenBundles 仅兜 import 时报错这种静态看不见的场景）
    → ③补丁降级 full→bridge→none。
  - `restoreQuarantinedBundles()` 恢复前逐个 bundleBootable：仍坏的保持隔离并说明原因，
    不再"无脑加回→启动又崩→再隔离"；无实际恢复时不重启内核。
- **验证**：
  - 单测 `lib/profile-inspect.test.js` 7 项全过（npm run profile-inspect-test）；
  - 真实 profile 前置体检干净（5 插件全 bootable，无误报）；
  - 端到端：往真实 profile 临时注入 `@dsh-local/dsh-broken-test`（缺 node_modules 条目，
    已先备份）→ 隔离 userData 跑 `npm run smoke` → **SMOKE_OK**，坏插件被确定性识别
    （原因精确："node_modules 条目缺失或悬空"）、从 bundles 摘除、进隔离名单、
    内核正常启动 → 事后还原 profile 与备份一致（7 bundles，无残留）。
  - `npm run smoke` 正常态亦通过（前置体检无误报、应用正常启动）。
- **坑**：无新坑；沿用 ㉑ 的隔离 userData（DSH_DESKTOP_USER_DATA）绕单实例锁的旁路法。

— 署名：deepseek-v4-flash

## ㉝ 架构：统一原子化持久层（settings / manifest / 补丁写侧）

- **背景**：续 ㉜ 继续架构优化。审查发现事故史里反复出现的"写坏文件"类问题
  （㉒ bundles 重写丢条目、㉓ BOM 崩内核、settings 写一半崩回落默认）根子都在
  "直接 writeFileSync 覆盖 + 无容错读取"这一层——每个调用点各自为政。
- **正确逻辑**：抽一层共享原子持久化基元，所有关键文件统一走它：
  - 新增 `lib/atomic-file.js`（纯 node）：`writeFileAtomic`（临时文件 + 同卷
    rename 原子替换，读方永远见完整旧/新文件，无半截）、`writeJsonAtomic`、
    `readJsonSafe`（剥 BOM + 解析失败返回 null 不抛）、`backupFile`。
  - 新增 `lib/settings-store.js`（纯 node）：设置存储单点——默认值合并（新版本加
    默认项自动补全旧磁盘文件）、BOM/损坏容错、原子写、migrate 钩子。
  - `main.js` 接线：settings 读写 → settings-store；隔离/恢复写 manifest 与隔离名单
    → writeJsonAtomic；kernel.patch.yml → writeFileAtomic；readJsonStripBom 委托
    readJsonSafe（profile-inspect 同步去重）。
  - `package.json` files 收录两个新 lib（打包版依赖），新增 `atomic-file-test` 脚本。
- **单测**：`lib/atomic-file.test.js` 14 项（原子覆盖无 .tmp 残留/多级目录/BOM/半截
  文件回落默认/默认值合并/migrate 等），`profile-inspect-test` 7 项回归，全绿。
- **坑（重要）**：接线时踩了初始化顺序 bug——`let settings = loadSettings()` 在
  `const settingsStore = ...` **之前**执行，loadSettings 引用 const 触发暂时性死区
  （`Cannot access 'settingsStore' before initialization`），应用直接加载失败。
  真实冒烟 `SMOKE_FAIL` 逮住 → 把 `let settings` 移到 settingsStore 初始化之后。
  **教训：const 在模块顶部被"前置调用"引用时一定注意定义顺序；冒烟必须有真实启动
  兜底，单测覆盖不到 Electron 模块加载时序。**
- **验证**：`npm run smoke` SMOKE_OK（修复后）；settings.json 原子写后内容与用户
  原设置一致（theme=palis、windowBounds 保留）、无 .tmp 残留；四套 node 测试全绿。

— 署名：deepseek-v4-flash

## ㉞ 清理：extraResources 去重 + 陈旧构建残留移除

- **背景**：工作区排坑（见根目录 `日志/2026-08-22.md`）时发现本仓库两处缺陷，均属历史残留。
- **改动**：
  1. `package.json` extraResources 里 `plugin/dialog-optimize` 重复配置两段
     （from/to/filter 完全相同），且重复段与 shell-settings 条目缩进不一致——
     去重为一段、缩进拉齐。重复拷贝到同一路径产物等价，属配置级修正，
     不动 isPackaged 代码分支，无需重打安装包验证。
  2. 删除 `dist-alt/`（369MB：win-unpacked + win-unpacked.tmp，08-22 00:48 的临时
     构建残留）与 `dist-alt-config.json`（0.1.21 时代陈旧配置：output 指向
     %TEMP%\dsh-build-021，缺 shell-settings 与 lib/profile-inspect.js、
     lib/atomic-file.js、lib/settings-store.js 条目）。㉛「复制旧配置漏打
     shell-settings」正是这类存档导致的——临时构建配置用完即弃，需要时从
     package.json 的 build 节现生成，永远是新的。
- **改动文件**：`package.json`（去重）；删除 `dist-alt/`、`dist-alt-config.json`。
- **验证**：node 校验 package.json 可解析、extraResources 5 条无重复；
  `npm run selector-check` 全 PASS；`npm run smoke`（DSH_DESKTOP_USER_DATA 隔离实例）
  SMOKE_OK；atomic-file 14 项 / profile-inspect 7 项 / git-review 回归全绿。
- **坑**：无新坑。删除 dist-alt 释放 369MB；今后临时构建配置继续走 %TEMP%，不入库。

— 署名：kimi

## ㉟ 修复：PALIS 进度条竖排 + 内核启动抢开系统浏览器（用户报障两条）

- **问题 1：PALIS 启动画面进度条是垂直的**（用户截图：一整列蓝格竖排，行尾 `[ ] 100%`）。
  - 根因：`.ln-prog` 容器写了 `font-size: 0`（本意消格子缝隙）又写 `width: 20ch`——
    ch 是相对自身字号的单位，font-size:0 时 1ch=0px，容器塌成 0 宽，20 个
    inline-block 格子被迫逐一换行，叠成竖条。**出生即缺陷**：PALIS 皮肤落地提交
    1398836 即如此，不是回归，此前的回归断言与截图目检都未覆盖这条进度条。
  - 正确逻辑：容器不再用 ch 定宽——JS 恒生成 20 个 1ch 宽的 `<i>`，容器宽度由格子
    自然撑开，再加 `white-space: nowrap` 从机制上禁止换行；CSS 注释写明
    「不得给容器用 ch 定宽」防止回潮。
- **问题 2：桌面端启动后跳到系统浏览器，回桌面端要手动切**。
  - 根因：内核 `dsh web` 的 web-startup 默认 openBrowser=true，就绪后用系统默认
    浏览器打开 Web UI（工作区 `日志/kernel.log` 留有 6 条 "opening the default
    browser" 实证），与外壳原生窗口重复且抢焦点。
  - 正确逻辑：外壳的职责就是用原生窗口承载内核页面，浏览器交接永远不该发生——
    拉起内核的 baseArgs 固定带 `--no-open`（内核 web-startup 原生参数，README 明示语义）。
- **改动文件**：`renderer/splash.css`（.ln-prog）、`main.js`（baseArgs）。
- **验证**：
  - `npm run splash-check` 231 项全过；`splash-shot --theme=palis --states` 三态截图
    逐张目检：拉起中（41%）/ 就绪（100%）/ 失败（ABORT），进度条均横排在方括号内；
  - `node --check main.js` 过；`npm run smoke`（DSH_DESKTOP_USER_DATA 隔离实例）
    SMOKE_OK，隔离实例 kernel.log 的 launch 行已含 `--no-open`，全篇无
    "opening the default browser"。
- **坑**：无新坑。备注：本机浏览器若在今日早些时候的冒烟时弹出过 127.0.0.1 标签页，
  正是本修复消灭的行为（旧参数拉起内核所致），现已根除；已安装版要等下个发布才生效。

— 署名：kimi

## ㊱ 发布 v0.1.24

- **定版**：package.json 0.1.23 → 0.1.24；CHANGELOG [Unreleased] 归档为 [0.1.24] - 2026-08-22
  （内容 = ㉜ 前置体检 + ㉝ 原子持久层 + ㉞ 配置清理 + ㉟ 启动体验两条）。
- **提交**：`c64ba9b release: v0.1.24（…㉜㉝㉞㉟）`，11 文件 +659/-58，已推 origin/main。
- **构建**（ZCode 在跑，沿用 ㉗ 的 %TEMP% 外置构建法，输出 %TEMP%\dsh-build-024）：
  - 运行时重打包 dist/runtime.tar.gz 71.4MB（marker: dsh 0.1.1-rc.1）；
  - **临时配置改为从 package.json 的 build 段现场派生**（只改 directories.output）——
    从机制上消灭 ㉛「复制旧存档配置漏打新条目」的事故形态，配置永不过期；
  - electron-builder --dir → app-update.yml → NSIS --prepackaged → gen-update-manifest.js，
    产出 setup.exe 190,803,446 B + blockmap + latest.yml（version 0.1.24，sha512 一致）。
  - 产物校验：resources/plugin 三插件齐（shell-settings 三件套在）；app.asar 含
    atomic-file.js / settings-store.js / profile-inspect.js / settings.html。
- **发布**：`gh release create v0.1.24`（🐛/⚙️ 分类说明）+ 三件套上传，
  资产尺寸与本地逐字节一致；tag 指向 main 上的 c64ba9b。
  https://github.com/yuanzhoucanxiang/dsh-desktop/releases/tag/v0.1.24
- **macOS**：build-macos 工作流已由 tag 触发（run 32564926375，进行中）。
  注意 v0.1.22/v0.1.23 两次 macOS 构建均为 failure——存量问题，本次未动，待专项排查。
- **坑**：无新坑。ZCode 文件监视仍在跑，工作区内构建仍会撞 asar 锁，外置构建法继续有效。
- **装机生效**：已装 0.1.23 可在托盘「检查更新…」拉到 0.1.24（0.1.22+ 的新更新状态机
  含断线重试+进度条，本次正好是它第一次真实大版本下载检验）。

— 署名：kimi

## ㊲ 设置面板接入外壳皮肤体系（用户："这个界面的 ui 风格没有统一"）

- **背景**：设置面板（插件体检/软件更新）此前是固定深海配色（settings.css 注释自述
  "固定深海配色"），托盘切 seascape/palis 后面板不跟——外壳自有的四个界面
  （启动画面/注入侧栏/浮层/设置面板）里唯独它掉队。
- **正确逻辑**：不重写样式，而是把 settings.css 全部硬编码颜色/圆角/字体收编为
  设计令牌（:root 默认值 = deep，与原观感一致），seascape / palis 纯换令牌 +
  少量结构修饰——与启动画面同一体系、同一纪律：
  - seascape：单色银盐化（强调色=亮银，主按钮银底墨字，h1 衬线）；错误态沿用
    海景 splash 的低饱和暗红先例（语义色完全抹平会误事）。
  - palis：黑白高反差 + 系统蓝/警示红双强调、全等宽、直角、大写大字距 +
    极淡 CRT 扫描线 overlay。
- **桥接**：首帧皮肤由主进程开窗时在 URL 带 `?theme=`（零闪烁）；托盘切换经
  `shell:theme` 即时变（applyTheme 广播名单补进 settingsWin，此前漏了它）；
  渲染侧再用 status() 校正一次兜底。
- **改动文件**：`renderer/settings.css`（令牌化重写 + 双皮肤块）、
  `renderer/settings.js`（applySkin + 订阅）、`main.js`（开窗带 theme 参 +
  广播补 settingsWin）。
- **验证**：`DSH_DESKTOP_SETTINGS_PANEL=1` + `DSH_DESKTOP_SETTINGS_SHOT` 隔离实例
  E2E 截图 ×4（deep/seascape/palis 插件页 + palis 更新页）逐张目检：
  deep 与原观感一致（回归无损）；seascape 全银盐单色；palis 等宽直角蓝红+扫描线，
  与启动画面/内核 PALIS 同观感。`node --check` 双文件过；冒烟（隔离实例）SMOKE_OK。
- **坑**：① 复用同一隔离 userData 连跑两次 E2E，第二次面板窗没起来导致漏截——
  换新目录即过，E2E 截图要一次一目录；② 圆角令牌收敛为三档（--r-s/--r-xs/--r-m），
  deep 与原样式的圆角差异 ≤2px，肉眼不可见。

— 署名：kimi

## ㊳ PALIS 声线波动条（用户："蓝线模拟成蓝色的声线波动条，随 ai 思考和输出起伏"）

- **归属**：输入框顶边蓝线是 `@dsh-local/palis-theme-panel`（工作区独立插件项目，
  junction 进内核 profile）画的 `[data-composer-card] { border-top: 2px solid --palis-accent }`，
  不是内核也不是外壳——动效因此落在该插件里（v0.1.0 → v0.1.1）。
- **信号**：内核语义属性 `[data-streaming]`（assistant 消息根在推理/输出全程置位），
  语义契约级稳定，不碰 hash 类名/aria 文案；MutationObserver 120ms 节流扫描 +
  700ms 软着陆 + 输出突发密度加成振幅。
- **形态**：顶边叠加 canvas——静默清空（原蓝边=静止基线，观感零变化），
  活动时复合正弦波（两端收拢包络 + 呼吸伪噪声）；遵守 prefers-reduced-motion。
- **坑**：canvas 是替换元素，绝对定位 left+right 拉伸无效（塌成 36px），
  必须显式 `width: calc(100% + 2px)`；详见插件 WORKLOG 第 7 节。
- **验证**：隔离实例 CDP 连拍（wave-v-idle/stream1-4/after.png）——静默与结束后
  直线与原观感一致，流式帧全宽起伏；插件 typecheck+build 过。
- **改动文件**：`dsh-palis-theme-panel/src/client/index.ts`（波动引擎）、
  `src/theme-core.ts`（.palis-wave 定位样式）；插件自身 CHANGELOG/WORKLOG 已同步。

— 署名：kimi

## ㊴ PALIS 声纳扩散（用户："中间的圆点类似声纳扩散出去的特效，呼应声线波动"）

- **靶心**：背景环形轨道图是 `[data-conversation-scroll]` 的 CSS 背景（ART_ORBIT SVG），
  局部动效做不了——正确逻辑 = client DOM 层 `.palis-sonar`（三环错峰 scale + 中心蓝点脉动），
  按背景定位公式反解圆心对位（cx=50%W，cy=(H−0.7W)·0.58+0.35W），挂 `[data-phase]` 根容器。
- **联动**：与声线波动条共用 `[data-streaming]` 活动门（新增门闩翻
  `html[data-palis-activity]`）——静默 6.4s 深空慢 ping，活动 2.3s 密集快 ping。
- **验证**：隔离实例 CDP——对位像素级吻合、活动门随流式正确翻转、截图目检通过
  （`verify-out/sonar-*.png`）；插件 build 过。
- **改动**：`dsh-palis-theme-panel/src/{theme-core.ts, client/index.ts}`（插件 v0.1.1，
  其 CHANGELOG/WORKLOG §8 已同步）。

— 署名：kimi

## ㊵ PALIS 声纳迭代：修方环 + 提亮扩大 + 原轨道环联动（用户实机反馈）

- **方环根因**：主题直角模式的全局 `border-radius:0 !important` 误伤声纳环——豁免复圆。
- **提亮扩大**：ping 环 520→760px、峰值透明度 .45/.85 两档、亮蓝描边 + 光晕。
- **联动消割裂**：中心蓝点静默呼吸/活动脉动；新增同径旋转虚线环（mask + live 色值，
  JS 定径 0.368·S）盖静态蓝环 = 原环转动（48s/活动 10s）。
- **验证插曲**：旋转元素的 bounding rect 含 rotate() 变换（216×√2=305），
  一度误判尺寸 bug——实际内联尺寸与 RO 跟随一直正确。
- **交付**：按新约定（根 AGENTS.md 第 5 条）刷新可见预览实例给用户实机查看。
- **改动**：`dsh-palis-theme-panel/src/{theme-core.ts, client/index.ts}`（仍 v0.1.1，
  其 CHANGELOG/WORKLOG §8a 已同步）。

— 署名：kimi

## ㊶ PALIS 轨道旋转 + 行星公转：中心圆环交替转向，白点变行星（用户实机反馈）

- **五环交替旋转**：声纳层叠 5 个与各静态轨道环同径的 mask 虚线环（蓝 r=184 +
  灰 r=348/264/96/30），JS 逐帧积分角度，ω 走双频正弦叠加——符号自然翻转 =
  不规律顺/逆时针交替；大环慢、小环快。盖在静态环上 = 原来的元素真的动了。
- **定径修正**：mask 圆半径占元素边长 47%，边长 = S·r/470（上一版直取 r/1000 偏小 6%）；
  虚线节奏也按各环 SVG dasharray 折算，对齐时与静态环无缝。
- **行星公转**：ART_ORBIT 抠掉 8 个静态白点（不重影），DOM 行星接替——正交 4 颗巡
  r=430、对角 4 颗巡 r=294（原角位作初相），开普勒式内快外慢，另加 1 颗蓝色卫星巡
  蓝环；静默 4 分钟/圈持续慢转（独立 rAF，不随波动条休眠），活动把角速度 ×(1+3·heat)。
- **坑**：TaskStop 只杀 npm 包装进程，electron 子树残留占单实例锁致新实例静默退出——
  换载预览须 taskkill //T 整棵树（按命令行路径区分，勿碰正式版 Desktop 进程）。
- **验证**：verify-orbit.mjs 全过（5 环 9 行星、半径像素级吻合、两采样点皆在动、
  静态白点已抠）；可见预览实例已刷新交付。
- **改动**：`dsh-palis-theme-panel/src/{theme-core.ts, client/index.ts}`（仍 v0.1.1，
  其 CHANGELOG/WORKLOG §8b 已同步）。

— 署名：kimi

## ㊷ PALIS 数据天体卫星呼应：球区动效 + 球体复圆（用户实机反馈）

- **卫星呼应**：球旁新增倾斜轨道卫星——公转周期 = 球自转 1/5、方向与球面漂移一致、
  轨道面 240s 进动；真 3D 遮挡（后半程落进球盘被遮蔽淡出，盘外压暗，近大远小）。
- **活动门进 globe 引擎**：球自转/卫星/进动共用 boost ×(1+2·heat)，与声纳/行星同节奏。
- **顺带修复**：方形模式把 `.palis-globe-sphere` 和 HUD 环 r1/r2 切成方（探针实测
  square=on 下 sphere radius=0）——具象图形按声纳环同例豁免复圆。
- **验证**：verify-globe-sat.mjs 全过（位移/遮挡周期 0↔0.95/进动/圆度）；
  可见预览实例已刷新交付。
- **改动**：`dsh-palis-theme-panel/src/{theme-core.ts, client/index.ts}`（仍 v0.1.1，
  其 CHANGELOG/WORKLOG §8c 已同步）。

— 署名：kimi

## ㊸ PALIS 视觉平衡：静态中心压暗 + 动效提亮（用户实机反馈）

- **压暗**：轨道图中心 r=96/r=30/中心点三件套与蓝环描边降透明度（静态退后让位内容）。
- **提亮**：ping/中心蓝点/五档旋转环/行星/卫星轨道环全线上调（含尺寸 5→6px、
  光晕加强、活动档上限拉至 .95~1）。
- **验证**：verify-orbit.mjs 全过；截图复核观感。预览实例已刷新。
- **改动**：`dsh-palis-theme-panel/src/theme-core.ts`（+client 卫星亮度参数，仍 v0.1.1，
  其 CHANGELOG「调整」段/WORKLOG §8d 已同步）。

— 署名：kimi

## ㊹ PALIS 细节打磨：六环/ping 定径/轨道环遮挡/live 读数

- **六环补齐**：最外圈 r=430 旋转覆盖环（g4）到位，整套轨道环全部交替旋转。
- **ping 定径**：max(760px, 1.1·S)，超宽屏也越过最外轨道环。
- **遮挡一致**：卫星轨道环移到球体之下，掠过球盘的弧段被球遮蔽（与卫星遮挡一致）。
- **live 读数**：HUD 死文字改 globe 引擎 500ms 覆写——LON 随自转真实走动、
  SIGNAL 活动降延迟、TRACK = 真实动体数量、INDEX hex 跳动。
- **验证**：verify-orbit.mjs（六环/g4 半径/ping 定径）+ verify-globe-sat.mjs
  （遮挡周期/层序/读数跳动）全过；预览实例已刷新。
- **改动**：`dsh-palis-theme-panel/src/{theme-core.ts, client/index.ts}`（仍 v0.1.1，
  其 CHANGELOG「细节打磨」段/WORKLOG §8e 已同步）。

— 署名：kimi

## ㊺ PALIS 球面质感重写：双线性采样 + 方向光照 + 临边辉光（用户实机反馈）

- **块状感消除**：最近邻 → 双线性采样（灰度贴图只取 R 通道，x 向位与环绕）。
- **方向光照**：固定光源左上前方，昼 .92/夜 .2 逐像素明暗（预计算，渲染零三角）。
- **临边**：(1-nz)³ 偏蓝大气辉光 + 6px alpha 柔边；CSS 遮光层减负（删 .93 重暗化）。
- **验证**：verify-globe-texture.mjs 像素级断言全过（昼/夜 163/40、蓝偏置 +34、
  柔边 alpha 137）；截图目视体积感成立。预览实例已刷新。
- **改动**：`dsh-palis-theme-panel/src/{client/index.ts, theme-core.ts}`（仍 v0.1.1，
  其 CHANGELOG「细节打磨」段/WORKLOG §8f 已同步）。

— 署名：kimi

## ㊻ PALIS 球面质感方向修正：弱化光照，强化构成元素（用户实机反馈）

- **光照降回暗示**（昼夜 1.13×，临边只勾边）；双线性采样/柔边保留。
- **构成元素加强**：等高线加密提亮（9 条 .17）、经纬网/半调网点/陆块阶梯上调、
  新增数据刻度（8 测量十字 + 2 标记环，随球旋转）；表面粒子三层异相 + 蓝火花。
- **验证**：verify-globe-texture.mjs 全过（扁平光照/线密度/勾边/柔边/三层尘埃）；
  预览实例已刷新。
- **改动**：`dsh-palis-theme-panel/src/{theme-core.ts, client/index.ts}`（仍 v0.1.1，
  其 CHANGELOG「细节打磨」段/WORKLOG §8g 已同步）。

— 署名：kimi

## ㊼ PALIS 球面去光照：固定曝光纯纹理（用户实机反馈）

- **去光照去辉光**：8f 引入的逐像素方向光照与临边蓝色大气辉光整体删除，
  render 改固定曝光 ×0.55 压暗；临边只留 alpha 柔边（抗切边，非光效）。
- **纹理不变**：双线性采样、贴图构成元素（等高线/经纬网/网点/数据刻度）、
  三层异相表面粒子、卫星层、live 读数全部保持。
- **验证**：verify-globe-texture.mjs 断言改向后全过——昼夜 96/97（1.01×，
  原 161/143）、临边蓝偏置 0（原 +21）、柔边/线密度/尘埃保持；截图目视
  均匀暗灰纹理球。预览实例已刷新。
- **改动**：`dsh-palis-theme-panel/src/client/index.ts`（+ theme-core 注释同步，
  仍 v0.1.1，其 CHANGELOG「细节打磨」段/WORKLOG §8h 已同步）。

— 署名：kimi

## ㊽ PALIS 球面去柔边：硬切边（用户实机反馈）

- **去柔边**：临边 alpha 渐隐预计算（alphaArr）整体删除，圆盘像素写死 255，
  硬切边（边缘交给 CSS 圆形容器裁剪 + 细描边）。球面"软"手段（光照/辉光/柔边）
  至此清零。
- **探针标定修正**：曝光 ×.55 后扫行跳变阈值 14 漏检经纬网（度量失真，非纹理
  回归）——降 8 后跳变 16 次，断言 softLimb → hardLimb。
- **验证**：verify-globe-texture.mjs 全过（昼夜 96/102、蓝偏置 0、硬边 255/255、
  线密度/尘埃保持）；截图目视边缘干脆。预览实例已刷新。
- **改动**：`dsh-palis-theme-panel/src/client/index.ts`（仍 v0.1.1，其
  CHANGELOG「细节打磨」段/WORKLOG §8i 已同步）。

— 署名：kimi

## ㊾ PALIS 球面深压暗：对标早期暗色观感（用户实机反馈）

- **曝光 ×0.55 → ×0.32**（实测亮度 96→56）：8h 折算漏算了 8f 已删的重 CSS 遮光，
  等效曝光远高于早期版本。
- **线元素补强**：等高线 .26/经纬网 .12/网点 .14/刻度 .36——绝对线亮度不降，
  基底降 42%，暗底清晰网点回归。
- **探针抗相位**：单行跳变随自转相位波动（16/5/2），改 3 行求和阈值 ≥10
  （实测 10/12 稳过）；darkened 收紧 <80。
- **验证**：verify-globe-texture.mjs 全过；截图与用户对标的早期暗色图同区间。
  预览实例已刷新。
- **改动**：`dsh-palis-theme-panel/src/{theme-core.ts, client/index.ts}`（仍
  v0.1.1，其 CHANGELOG「细节打磨」段/WORKLOG §8j 已同步）。

— 署名：kimi

## ㊿ PALIS 球面多尺度网点体系（用户给原作参考图）

- **参考 PALIS 总目录屏的粒子密度搭配**：单层 4px 网点 → 四层（细粒 3px 底噪 /
  中粒 4px / 粗疏粒 9px 暗区散布 / 陆块密度调制层 5px——以陆块湍流明度作 mask，
  陆地网点加密、海洋稀疏，密度跟明度走）。
- 贴图栅格化一次性结算，渲染循环零新增开销。
- **验证**：verify-globe-texture.mjs 全过（56/61、蓝偏置 0、硬边 255、跳变 13、
  尘埃 3 层）；原尺寸截图目视层次拉开。预览实例已刷新。
- **改动**：`dsh-palis-theme-panel/src/theme-core.ts`（仍 v0.1.1，其
  CHANGELOG「细节打磨」段/WORKLOG §8k 已同步）。

— 署名：kimi

## 〔51〕 PALIS 球面纹理质感继续加强（用户实机反馈）

- **梯田化陆块**（阶梯 6→7 档）、**等高线 9→13 条 + 位移 60→85**、**新增 8 条
  纵向波纹线**（正交编织，纵向不跨接缝天然无缝）、刻度 12 十字 + 3 环、噪点 .15。
- **修正对比衰减**：线叠在亮陆块上实际增量仅 ~5 亮度被冲淡——等高线/波纹线/
  网点对比补强（.3/.2/.18/.3）。
- **探针换代**：跳变计数随相位漏检（16/5/2/4），换纹理能量（相邻亮度差均值
  ×100，实测 131~141，阈值 ≥100）；noLighting 放宽 1.08→1.2（颗粒噪声所致）。
- **验证**：verify-globe-texture.mjs 全过；截图颗粒更脆、层次可见。
  预览实例已刷新。
- **改动**：`dsh-palis-theme-panel/src/theme-core.ts`（仍 v0.1.1，其
  CHANGELOG「细节打磨」段/WORKLOG §8l 已同步）。

— 署名：kimi

## 〔52〕 PALIS 球面纹理对比加强（用户实机反馈）

- **对比拉伸曲线**进渲染循环：`((s-128)×1.4+128)×E`，绕中灰拉开——暗部更暗、
  亮部更亮、均值近似不变（不抬整体亮度）；uint8 写入自动截断，零预计算变更。
- 优于继续堆 alpha：曲线对全部构成元素（含湍流基底梯田）一视同仁拉开反差。
- **验证**：verify-globe-texture.mjs 全过——纹理能量 131~141 → 177（+30%）、
  昼侧 58→64 仍低于压暗线、其余指标不变。预览实例已刷新。
- **改动**：`dsh-palis-theme-panel/src/client/index.ts`（仍 v0.1.1，其
  CHANGELOG「细节打磨」段/WORKLOG §8m 已同步）。

— 署名：kimi

## 〔53〕 侧栏动效衔接 + better-sidebar 构建链事故重建（窗口切换优化轮）

- **需求**：优化动效与窗口切换衔接（左右侧边栏/底部面板开合）。
- **动效改动**：
  - `dsh-palis-theme-panel`：PALIS_CSS 新增动效节奏块，主题开启时覆盖内核
    `--ds-transition-duration .16s / -fast .08s / -slow .24s` +
    `--ds-ease-in-out: cubic-bezier(.3,.85,.25,1)`——侧栏/面板/控件全局同步。
  - `dsh-better-sidebar`（0.12.2）：`.panelBody` 交叉淡化（展开 .16s 延迟 84ms
    淡入/收起 .08s 淡出）、toggle 按钮 `data-open` 状态指示（accent 着色 +
    图标过渡）、reduced-motion 豁免同步。
- **事故（自曝）**：在 `dsh-better-sidebar/package` 跑 `npm run build`，脚本首步
  `rm -rf lib` 删除预构建产物后 tsc 因缺 `tsconfig.build.json` 失败——lib/ 丢失，
  内核前置体检自动隔离插件（Temp/dsh-preview/disabled-bundles.json + profile
  package.json bundles 列表被摘除）。npm registry 不通、非 git 仓库，无法找回原版。
- **重建（正确逻辑覆盖，非打补丁）**：
  - 新写 `tsdown.config.ts`：host ESM（deps 全外部化）+ client 核心 CJS
    （`__ModuleLoader__.load` 包装，与官方内核 bundle 同构）+ terminal/editor
    懒加载分包（`__dshChunks__` 包装）；CSS modules 自实现插件经虚拟 id 绕开
    tsdown css-guard，类名按文件路径 sha1 作用域化 + 运行时 `<style>` 注入。
  - 依赖策略修正：tsdown 默认外部化 dependencies，改 `alwaysBundle` 谓词——
    平台 seed 词（react 系 + `@deepseek-ai/*`）保持外部，codemirror/xterm/clsx
    等全部打进包（否则浏览器端 require("clsx") miss module table）。
  - **真缺陷修复**：懒加载分包依赖 `window.__DSH_MODULES__`，但当前内核从未安装
    该全局（终端/编辑器分包一直加载失败）。平台实际经 cordis 发布模块系统
    （`ctx.modules`，dsh-client-modules apply 里 reflect.provide）——改为 client
    入口 apply() 显式 `provideChunkModuleSystem(ctx.modules)`，inject 加 modules。
  - 补 `tsconfig.build.json`（emitDeclarationOnly → lib/types，73 个 .d.ts），
    build 脚本顺序改 `rm -rf lib && tsdown && tsc`；修复 node_modules 189 个失效
    junction（原指向已打包成 runtime.tar.gz 的旧安装目录 → 工作区 runtime）。
    剩 6 个 client 平台包本机无类型源（内核从打包资产服务），tsc 对此报 TS2307
    但声明产全，运行时无影响——记录在案。
  - 隔离解除：清空 disabled-bundles.json + bundles 列表恢复 dsh-better-sidebar。
- **验证**（预览实例 v21，kernel 8770）：verify-motion.mjs 8/8 全过（令牌生效、
  data-open、面板 0.24s、交叉淡化延迟、收起中段 matrix/opacity 真实中间帧）；
  终端分包加载实测通过（xterm 挂载、无 chunk 报错）；截图 verify-out/motion.png。
  用户正式安装（5268）共用同一 profile，重启后同受益。
- **改动**：`dsh-better-sidebar/package`（src client 动效 + chunk-loader/index/
  context-types 模块系统交接 + tsdown.config.ts/tsconfig.build.json/package.json
  build 脚本）、`dsh-palis-theme-panel`（theme-core.ts 节奏块，CHANGELOG 细节打磨
  段/WORKLOG §9 已同步）。

— 署名：kimi

## 〔54〕 侧栏开合闪烁治理：margin 同帧 + 合成层稳定 + 波动条防抖（用户实机反馈）

- **需求**：右侧边栏弹出/收回时发生闪烁，继续整体优化。
- **逐帧取证**（headless Chrome screencast，帧存 verify-out/flicker/）定位三条
  根因：
  1. **margin 过渡晚起一帧**（better-sidebar）：`--dsh-sidebar-width/height`
     原在 `useEffect`（paint 后）写入 documentElement，面板 class 翻转在 commit
     前 → margin 过渡比面板滑移晚起一帧，边缘错位实测 ~10px。改
     `useLayoutEffect` 与 class 翻转同帧生效，复测错位 ≤7px（残余为逐帧采样
     量化，非真实错位）。
  2. **合成层升层闪帧风险**（better-sidebar，预防性）：`.panel`/`.bottomPanel`
     加 `will-change: transform`，滑移全程独立合成层，避免中段升/降层光栅化
     闪帧。
  3. **波动条清零风暴**（palis）：侧栏推拉/窗口缩放期间 ResizeObserver 逐帧
     触发，`canvas.width=` 赋值按 spec 清空画布 = 蓝线闪断。新增
     `scheduleWaveResize` 落定防抖（`WAVE_RESIZE_SETTLE_MS=160`，落定 160ms 无
     新尺寸才重置；`lane.w===0` 首次挂载仍立即），拆除路径同步清 timer。
     复测动画窗口内 `backingChangesDuringAnim: 0`（修复前每次 resize 均 >0）。
- **够不着的来源（如实记录）**：桌宠 dsh-pet（第三方打包插件，不在工作区）在
  toggle 后 ~0.4s 有一次 opacity 1→0.02→1 的自主眨眼（~200ms），z-40 也会被
  z-50 面板瞬时遮/露——其自身拟人化行为逻辑，不修；若观感困扰建议向其作者
  反馈。
- **验证**：预览实例 v21（kernel 8770）verify-motion.mjs 8/8 全过（令牌生效/
  data-open/面板 0.24s/交叉淡化延迟 84ms/收起中段 matrix 位移 + opacity 0.003
  真实中间帧）+ flicker 逐帧复测（错位 ≤7px、波动条 backing 零重置）。
- **改动**：`dsh-better-sidebar/package/src/client/{Sidebar.tsx, sidebar.module.css}`
  （仍 0.12.2）、`dsh-palis-theme-panel/src/client/index.ts`（仍 0.1.1，其
  CHANGELOG「细节打磨」段/WORKLOG §10 已同步）。改动均 client 侧，预览内核
  无需重启。

— 署名：kimi
